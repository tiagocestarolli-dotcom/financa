# Notas para um caminho comercial futuro

> Documento vivo. **Não afeta o desenvolvimento atual.** Atualizado conforme decisões relevantes vão sendo tomadas durante a construção do Finança.

---

## Propósito

Várias escolhas do projeto funcionam ótimo pra um usuário (Tiago) ou uma família, mas precisariam ser repensadas pra abrir o app comercialmente. Este arquivo registra cada uma dessas escolhas para revisita futura, sem desviar foco do trabalho atual.

---

## Decisões que precisam revisita pra escala comercial

### 1. Arquitetura single-file
**Hoje:** `index.html` único com ~10000 linhas (CSS + HTML + JS). Funciona bem porque é uma pessoa editando, deploy simples via GitHub Action.
**Comercial:** Inviável com equipe de mais de 1 dev. Migração pra build process (Vite/Webpack), componentização (React/Vue), code-splitting. Quebra do single-file também permite testes unitários por componente.

### 2. Schema "família única"
**Hoje:** State com keys hardcoded: `casa`, `marina`, `heitor`, `juliana`, `tiago`. Tiago é isolado por design (`isolado: true`).
**Comercial:** Conceito de família é forte e diferenciador, mas precisa virar configurável. Cada workspace teria N membros (sem nomes hardcoded), com tipos: titular / dependente / isolado / cônjuge. Subcategoria "exclusiva por categoria" generaliza bem.

### 3. Firestore como source-of-truth
**Hoje:** Documento `users/{uid}` com estado inteiro embutido. Realtime sync, persistência offline, simples.
**Comercial:** Custo escala mal (cada read/write é cobrado). Estado inteiro no doc limita a ~1MB por usuário, pouco com 5+ anos de gastos. Migração natural: Supabase ou PostgreSQL, com sync de realtime via solução separada.

### 4. Auth limitada a Google
**Hoje:** Só sign-in com Google.
**Comercial:** Email/senha, Apple Sign-In (obrigatório se for pra App Store iOS), recuperação de conta.

### 5. Multi-tenant não existe
**Hoje:** Cada usuário Firebase tem seu próprio documento isolado. Etapa B do roadmap (Juliana como segunda usuária) endereça parcialmente.
**Comercial:** Workspaces, convites, papéis (admin/membro/leitor), faturamento por workspace ou por usuário. Decisão estrutural cedo — repensar antes de virar débito técnico.

### 6. Onboarding zero
**Hoje:** App assume que usuário entende "gasto fixo", "subcategoria exclusiva", "grupo cross-cutting", "categoria isolada". Faz sentido pra Tiago, que desenhou.
**Comercial:** Tutorial primeiro-uso, dicas contextuais, defaults inteligentes (talvez detecção de banco do email pra pré-cadastrar contas), copy descomplicado.

### 7. Decisões opinionadas de produto
**Hoje:** Tiago é categoria isolada. Itaú default. Investimentos têm subcategoria fixa. Decisões assumem o perfil do Tiago.
**Comercial:** Tudo configurável via wizard de setup ("Quer uma categoria isolada da família? Quem? Qual seu banco principal?").

### 8. PWA-only, sem app nativo
**Hoje:** PWA via service worker inline. Funciona bem no iPhone, mas notificações push iOS são restritas, sem widgets de tela inicial.
**Comercial:** Considerar Capacitor (wrappers iOS/Android sem reescrever) ou React Native pra apps nativos. App Store/Play Store presence importam pra credibilidade.

### 9. Privacidade e LGPD
**Hoje:** Dados criptografados em trânsito (HTTPS) e em repouso (Firebase). Suficiente pra uso pessoal.
**Comercial:** Termos de uso, política de privacidade, encarregado de dados (DPO), processo formal de exclusão de conta, registro de operações de tratamento. Dados financeiros podem ser interpretados como sensíveis pela LGPD em alguns contextos.

### 10. Custos por usuário
**Hoje:** Firebase free tier cobre 1 usuário sobrando.
**Comercial:** Modelagem de custo por usuário ativo (Firestore reads/writes/storage, Auth, Hosting, eventual processamento server-side de PDF). Definir tier free vs pro.

### 11. Aliases de classificação (memória global por usuário)
**Hoje:** Os aliases aprendidos (descrição → categoria/subcategoria/grupo/identificação/isFixo) vivem no `state.aliasItems` do usuário. Quando Tiago atribui "DROGASIL 4940" como Casa·Saúde, todo usuário individual numa importação futura recebe a sugestão. A partir de v0.11.x, aliases também memorizam apelido livre (identificação) e flag de recorrência (isFixo).
**Comercial:** Aliases poderiam ter 3 camadas:
- **Globais (curadoria)**: "DROGASIL", "NETFLIX", "iFood" pré-cadastrados pela plataforma com categorias-padrão. Reduzem fricção do onboarding.
- **Por workspace**: aprendidos pela família.
- **Por usuário**: refinamentos pessoais.
Decisão de quando empurrar a sugestão global afeta a UX. Também: aliases globais geram dados agregados de comportamento de consumo — valioso comercialmente mas exige cuidado com privacidade/anonimização.

### 12. Parser PDF rodando no client
**Hoje:** PDF.js extrai texto no navegador do usuário. O PDF nunca sai do dispositivo.
**Comercial:** Excelente como diferencial de privacidade — pode virar argumento de marketing. Mas se quisermos suporte a mais bancos com parsers complexos (Nubank, BB, Caixa, C6, Bradesco, Santander, etc), processar server-side pode ser mais escalável (atualizar parsers sem republicar o app). Trade-off: privacidade vs manutenibilidade. Talvez híbrido: parsers comuns no client, fallback server-side opt-in.

### 13. Data de vencimento como data do gasto (não data da compra)
**Hoje:** Gastos de cartão entram no app com `data = data de vencimento da fatura`. A data da compra fica preservada em `dataCompra` apenas como flag visual.
**Comercial:** Decisão pragmática que funciona pra Tiago (faz contabilidade mental por mês de cobrança), mas pode confundir outros usuários que pensam por mês de uso. Apps comerciais geralmente oferecem opção: visualizar por data de compra OU por data de cobrança. Considerar toggle em Config.

### 14. Heurística de extração de chave do alias
**Hoje:** Primeira palavra significativa da descrição bruta, com fallback pra 2 palavras se a primeira for curta (≤4 chars). Remove prefixos comuns (IFD*, PG*, PD, DL*, etc.) hardcoded.
**Comercial:** Lista de prefixos é específica do mercado brasileiro e dos bancos cobertos. Internacionalização exigiria curadoria por região. Talvez ML simples (frequência de tokens) substitua heurística manual em escala.

### 15. Sem deduplicação automática de gastos importados (caso fatura)
**Hoje:** Se Tiago reimportar a mesma fatura de cartão, os gastos viram duplicados. Solução é manual (excluir os antigos).
**Comercial:** Detecção automática de duplicatas (mesmo cardId + valor + data ± 2 dias) com flag "já existe" na tela de revisão. Considerada e adiada pra Fatia 2.2 caso vire necessidade real. Pra comercial é mandatório — usuário leigo não vai entender por que tem 4 iFoods quando importou 2× sem querer.

### 16. Extrato bancário como "gap-filler", não fonte primária
**Hoje:** Etapa 5b ainda não implementada, mas a arquitetura está decidida: extrato vai entrar como **complemento** dos gastos manuais/importados de cartão, não como fonte total. Workflow desenhado para implementação:
- Usuário escolhe o período do extrato a importar. Default = "desde o último lançamento na conta + 1 dia" até a data do extrato.
- Parser sugere classificação por tipo de cada linha: entrada-renda, entrada-transferência, saída-consumo, saída-transferência, saída-pagamento-de-fatura, saída-tarifa.
- Tela de revisão flagra duplicatas contra `state.gastos` e `state.transferencias` da mesma `contaId`:
  - **Match estrito** (mesma conta + valor exato + data exata): "já cadastrado", desmarcado por default.
  - **Match aproximado** (mesma conta + valor exato + data ±2 dias): "possível duplicata", desmarcado por default, mostrando qual lançamento casou.
  - **Sem match**: marcado normal, pronto pra atribuir categoria.
- Aliases da Fatia 2.1 são reaproveitados: o que foi aprendido em fatura ajuda na classificação do extrato (e vice-versa).

**Comercial:** Isso é um **diferencial claro contra apps brasileiros típicos** (GuiaBolso, Mobills, Organizze) que importam tudo e geram duplicatas óbvias, deixando o usuário pra limpar manualmente. Argumento de marketing: "importe o extrato sem medo — só entra o que falta". O modelo de "gap-filler" também respeita melhor o usuário que registra manualmente em tempo real e não quer perder a sensação de controle.

**Pré-requisitos técnicos comerciais:**
- Dedup robusta é hard requirement (mais crítico aqui que no item 15, porque o caso de uso pressupõe co-existência de fontes). Em escala, pode aceitar variações: mesmo valor com 1 centavo de diferença (taxas residuais), descrições parecidas (fuzzy match).
- **Detecção de transferências pareadas** (saída em conta A + entrada em conta B mesmo dia mesmo valor = 1 transferência única, não 2 eventos) também é diferencial. Apps genéricos tratam como 2 lançamentos.
- **Detecção de pagamento de fatura** depende da Fatia 4 estar pronta (saída técnica vinda da `contaPagamentoId` do cartão). Por isso a ordem recomendada do roadmap é 4 → 5b.

### 17. Detecção de banco em fatura por marcadores múltiplos (logo é imagem rasterizada)
**Hoje:** Em fatura PDF do Itaú, "Itaú", "Personnalité" e CNPJ vêm como PNG embutido, não texto extraível pelo PDF.js. Detecção precisou cobrir marcadores secundários: nome do produto (`visa infinite`, `mastercard black`, que estão no corpo como texto) e telefones de atendimento (`4004 4828`, `0800 970 4828`). Cada banco novo (Sicoob, BTG, etc.) exige mapeamento manual desses marcadores.
**Comercial:** Trabalho linear por banco — pra escalar pra Nubank, BB, Caixa, C6, Bradesco, Santander seria necessário: (a) OCR fallback pra ler o logo quando texto falha, (b) catálogo server-side mantido pela plataforma de marcadores por banco, ou (c) cadastro guiado pelo próprio usuário ("qual seu banco?" antes de processar). Risco: banco muda layout ou marca, detecção quebra silenciosamente sem aviso. Em escala, monitoramento de "falhas de detecção" vira métrica de produto.

### 18. PDF.js fragmenta caracteres acentuados em items separados — normalização vira camada de infra
**Hoje:** Caracteres "ç", "ã", "é", "ó", "í" frequentemente vêm em items separados na extração. "Lançamentos" pode aparecer como 3 items: "Lan" + "ç" + "amentos". Quando agrupados por Y na reorganização, viram "Lan ç amentos" com espaços, e regex que depende deles quebra. Solução implementada: `normalizarFragmentacaoPDFJS(linha)` com regex que cola fragmentos em loop até estabilizar.
**Comercial:** Pra português brasileiro isso é universal — afeta qualquer parser de PDF processado via PDF.js. Em escala, normalização vira step obrigatório do pipeline de extração (não decisão caso-a-caso por parser de banco). Para outros idiomas com diacríticos (espanhol, francês, alemão), validar se a mesma fragmentação ocorre — provavelmente sim. Internacionalização passa por aqui.

### 19. Sistema de Y do PDF.js é nativo (origem inferior); PyMuPDF e outras libs server-side usam Y de tela (origem superior)
**Hoje:** Parsers que dependem de coordenadas Y (ex: detectar "Compras parceladas - próximas faturas" e cortar items abaixo desse marker) precisam saber em qual sistema estão. Hoje uso detecção adaptativa olhando item-âncora conhecido ("Resumo da fatura em R$"): se Y dele > mediana, sistema é PDF-native; senão, screen-native.
**Comercial:** Funciona desde que o item-âncora exista no documento. Bancos diferentes podem não ter "Resumo da fatura em R$" — cada formato exigiria mapeamento de âncora. Em escala: (a) catálogo de âncoras por banco mantido pela plataforma, (b) heurística independente de texto (ex: bounding box global da página), ou (c) abandono de PDF.js e migração pro server-side com biblioteca de Y previsível (PyMuPDF, pdfplumber). Última opção sacrifica o diferencial de privacidade do item 12.

### 20. Identificação (apelido 1-pra-1) vs grupo (agregador 1-pra-N) — escolha de schema com dois eixos
**Hoje:** Gasto pode ter `identificacao` (apelido livre tipo "Claude", "Netflix") OU `grupoId` (referência a um grupo cross-cutting tipo "Viagem Cancun"), ou ambos. Decisão explícita: não unificar — identificação discrimina 1 item dentro duma subcategoria; grupo agrega múltiplos gastos relacionados duma iniciativa.
**Comercial:** Terminologia pode confundir usuários leigos — "grupo" e "identificação" não são intuitivamente diferentes. Opções: (a) renomear identificação pra "apelido" ou "nickname", e grupo pra "projeto" ou "tag de projeto"; (b) unificar conceitualmente em "tag livre" e perder a granularidade do schema (perde-se a função de agregador automático do grupo); (c) explicar via onboarding com exemplos visuais. Trade-off entre simplicidade aparente e expressividade. Pra B2B (controle financeiro empresarial), a distinção tem valor — projetos vs centros de custo são conceitos contábeis distintos. Pra B2C, talvez unifique.

### 21. `isFixo: true` em gasto importado de fatura, sem `fixedExpense` espelho
**Hoje:** Implementei marca de fixo em gasto importado (assinaturas tipo Netflix, Claude, You Tube Premium) sem criar `fixedExpense` espelho — porque a fatura mensal já é a "engine" da recorrência, e criar fixedExpense duplicaria os lançamentos. Resultado: "fixo" agora tem 2 origens no sistema — replicação automática via fixedExpense ou flag direta no gasto. UI atual conta ambos juntos no filtro "fixos · X".
**Comercial:** Pra B2C funciona invisível — usuário não distingue origem. Pra B2B (especialmente controllers/contadores), pode precisar separar: "fixo recorrente automático" vs "fixo marcado manualmente" são auditoria-distintos. Pode virar 2 filtros separados, ou 1 filtro com sub-categorização visual. Schema preserva flexibilidade — campo `isFixo` é boolean simples, e `parentFixoId` identifica a origem (presente = clone de fixedExpense; ausente = marca direta). Decisão de UI fica pra revisão pré-comercial.

### 22. Descrição multi-linha em layout colunar — padrão Sicoob, pattern reutilizável
**Hoje:** Fatura Sicoob tem layout 4-col (DATA | DESCRIÇÃO | CIDADE | VALOR) com X fixos. Descrições longas (>~20 chars) quebram em 2-3 linhas físicas no PDF, e o PDF.js entrega cada linha como item em Y diferente. Lançamentos legítimos somem do parser genérico porque nenhuma linha sozinha matcha o regex `data + descrição + valor`. Solução implementada na Fatia 3.2: **âncora-de-DATA**. Cada lançamento é identificado pelo item na coluna DATA (X<50, texto "DD MMM"); todos os items dentro de |Y - Y_data| ≤ 5 pertencem ao mesmo lançamento (gap entre lançamentos é 18px); descrição multi-linha é concatenada ordenando os items da coluna DESCRIÇÃO por Y descendente. Funciona pra Sicoob, deve servir pra qualquer banco com layout colunar.
**Comercial:** Em escala (5-10+ bancos), virar **camada genérica do pipeline de extração**, não decisão caso-a-caso por parser. Detector de layout colunar via histograma de Xs (com freq ≥ 3), agrupamento por âncora-de-data com tolerância Y adaptativa (gap mediano + 1). O parser de cada banco fica responsável só por: (a) marcadores de detecção do banco, (b) filtros de descrições técnicas (anuidade, vantagem, juros, IOF), (c) regex de parcela X/Y caso varie de formato.

### 23. Floating-point edge case com Y exato na fronteira da tolerância
**Hoje:** Bug descoberto na Fatia 3.2: usar `Math.abs(it.y - anc.y) <= 4` (com gap de descrição multi-linha sendo exatamente 4) derrubava items legítimos por ruído de ponto flutuante (547.5000001 - 543.5 > 4 falsamente). Solução: sempre usar tolerância > gap medido (4 → 5).
**Comercial:** Risco silencioso — em testes com PDFs específicos pode passar, mas em outros falha. Em escala, o pipeline deve **medir o gap real por amostra** (mediana das distâncias entre items adjacentes em colunas com texto longo) e definir tolerância adaptativamente. Pra fail-soft: testes automáticos de regressão com auditoria de total bruto (item 26) pegam regressão imediatamente.

### 24. `parseValor` global do app não trata formato US com milhar
**Hoje:** A função `parseValor` (linha 5525 do index.html) é usada em todo o app pra interpretar valores monetários. Ela trata BR (1.234,56 e 50,00) e US-simples (50.00), mas falha em US com milhar (1,933.36): interpreta como "1.93336". Não causa bug no app hoje porque entradas manuais são sempre BR. **A fatura Sicoob usa formato US com milhar** — a Fatia 3.2 contornou implementando `parseValorSicoob` próprio no parserSicoob, sem mexer no global.
**Comercial:** Auditoria de débito técnico — em escala (com OFX, CSV de bancos internacionais, etc.) o `parseValor` global vai falhar silenciosamente. Refactor pendente: detectar formato pelo **último separador com 1-2 dígitos depois** (lógica já implementada em `parseValorSicoob`) e unificar. Risco de regressão moderado porque toca em todas as entradas monetárias do app — auditoria com testes de regressão antes do merge.

### 25. Anuidade do cartão como gasto vs encargo — decisão de produto opinionada
**Hoje:** Decisão da Fatia 3.2: anuidade entra como gasto (categoria a definir pelo usuário); vantagem progressiva (crédito redutor) e pagamento débito da fatura anterior ficam **de fora** da importação. Sicoob expõe anuidade parcelada (10/12, 11/12, etc.) — passa pelo aprendizado de alias como parcelamento normal e mês que vem auto-classifica.
**Comercial:** Pra B2C funciona — usuário não distingue se "anuidade" é consumo ou encargo, é só uma linha no gasto. Pra B2B (controllers/contadores), anuidade é fiscalmente "Despesas bancárias" / "Encargos com cartão", separada do consumo operacional. Pode precisar de categoria fiscal automática ("anuidade" → subcategoria "Encargos bancários" como sugestão de default). Esse tipo de regra-de-domínio cresce rápido; em escala, **biblioteca de regras de classificação fiscal** vira componente do produto B2B.

### 26. Auditoria de total bruto como gate obrigatório do pipeline de importação
**Hoje:** Convenção adotada nas Fatias 3.1 e 3.2: cada parser tem uma **"meta de auditoria"** — total dos lançamentos extraídos deve bater com o total bruto da fatura (extraído visualmente). Visa Infinite: 15 itens = R$ 12.972,27 (Fatia 3.1). Sicoob: 11 itens = R$ 1.947,92 (Fatia 3.2). Sem esse cross-check, regressões silenciosas passam (parser perdendo lançamento por um caractere a mais no regex, dupla-contagem de parcelas futuras, valor mal-parseado por 1 ordem de grandeza).
**Comercial:** Em escala (milhares de importações/dia em produção), vira **telemetria**: toda importação reporta `total_extraído / total_meta_estimado`. Discrepâncias >1% disparam alerta de "parser quebrou pra este banco/layout" — métrica de qualidade de produto. Também: na UI, exibir "✓ Total bate com a fatura: R$ X,XX" depois da importação aumenta confiança do usuário (especialmente em B2B onde auditabilidade é hard requirement). Hoje o app não mostra essa auditoria pro usuário; em escala comercial, virar feature visível.

### 27. Parser dual-mode (modo A com X/Y + modo B com texto bruto) — robustez genuína mas não foi a correção do bug Sicoob
**Hoje:** Durante debugging da Fatia 3.2 (15/maio/2026), achei que `pdfItems` chegava vazio em Safari iOS PWA (v0.12.0→v0.12.1), depois que modo A retornava lançamentos parciais (v0.12.2). **Ambos os diagnósticos estavam errados** — o `parserSicoob` nem foi chamado, porque o detector roteava pro Itaú genérico (ver item 29). v0.12.3 corrigiu o detector e o problema sumiu. **Apesar do diagnóstico errado, o dual-mode A/B e o best-of-N implementados continuam valendo como robustez genuína**: parser principal usa coordenadas X/Y (preferido, mais preciso, separa cidade da descrição); parser de backup usa textoBruto puro com âncoras "DD MMM" (mais robusto contra quirks de extração). Os dois modos rodam sempre, escolhe o de mais capturas, empate→A. Cobre cenários reais futuros: items chegando vazios por quirk de runtime, modo A perdendo casos específicos por sutil diferença de Y, ou layout diferente em uma fatura nova de outro banco.
**Comercial:** Em escala (milhares de devices, browsers, versões de SO), runtime quirks do PDF.js viram inevitáveis. **Pattern de "best-of-N" para parsers é replicável e simples**: cada modo independente, executa todos, escolhe o melhor por critério objetivo (contagem, soma vs total bruto extraído, etc.). Custo extra: ~milissegundos de CPU. Benefício: robustez sem precisar diagnosticar a causa raiz de cada quirk. Telemetria de "qual modo venceu por banco/SO" detecta regressões em produção. Em B2B isso vira SLA — "extração funciona em 99.5% dos PDFs" exige redundância.

### 27.1. Service Worker stale-while-revalidate vs deploy de patch
**Hoje:** Service Worker do app (linhas 10082-10122 do index.html) usa stale-while-revalidate com `ignoreSearch: true`. Resultado: query strings como `?v=12.3` NÃO forçam atualização. Quando um patch é deployado, usuário vê a versão antiga na PRIMEIRA visita pós-deploy (SW serve do cache, atualiza em background); a versão nova aparece SÓ NA SEGUNDA visita. Em ciclo de iteração rápida (4 patches em 1 sessão durante debug da Fatia 3.2), isso atrasa muito porque parece que "o fix não funcionou" quando na verdade só não foi servido ainda.
**Comercial:** Em B2C de uso casual (1x/semana), stale-while-revalidate é OK — usuário não percebe. Em B2C high-touch (uso diário) ou B2B (debugging com cliente), virar incidente. Soluções: (a) versionar `CACHE_NAME` em cada deploy (invalida cache imediatamente, mas força refetch completo); (b) detectar `?v=` no fetch handler e bypassar cache para essa request específica; (c) prompt "Nova versão disponível, recarregar?" mais agressivo (já existe mas o usuário pode ignorar). Decisão pré-comercial: balancear bandwidth vs imediatismo de updates.

### 28. Detecção de parcela X/Y em qualquer posição da descrição (não só no final)
**Hoje:** No modo B do parserSicoob (texto bruto sem coordenadas), a cidade fica colada na descrição depois da parcela ("IMUNOCENTRO SERV MED 01/05 BRASILIA"). Regex de parcela ancorada no final `/\s(\d{1,2})\/(\d{1,2})\s*$/` falhava nesse caso. Solução: regex global, valida atual/total (total>1, total≤99, atual≤total), pega o último match válido. Funciona pra parcela no fim (modo A) e no meio (modo B).
**Comercial:** Padrão recorrente em PDFs brasileiros — algumas operadoras (saúde, telefonia, faturamento parcelado) põem "01/05" antes do estabelecimento, antes da cidade, ou no meio do código de transação. Aplicar globalmente em parsers de extrato e fatura (não só Sicoob). Risco: falso-positivo se descrição naturalmente contém DD/MM válidos (improvável dentro do range de parcela). Cobertura de testes deve incluir descrições com números embutidos.

### 29. Ordem do detector de banco — emissor antes de bandeira/produto
**Hoje:** Bug encontrado nas v0.12.0, v0.12.1 e v0.12.2 (mascarado por hipóteses erradas durante 3 patches): detector verificava produto/bandeira ("Mastercard Black", "Visa Infinite") ANTES de verificar nome do emissor ("Sicoob", "BTG Pactual"). Faturas Sicoob com produto "Sicoob Mastercard Black Pro" eram roteadas pro Itaú genérico → parser genérico → 9 lançamentos em vez de 11, R$ 1.693,67 em vez de R$ 1.947,92. v0.12.3 inverteu a ordem: emissor primeiro (sinal único e forte), produto/bandeira só como fallback Itaú (justificado porque o logo Itaú é imagem rasterizada e não aparece no texto).
**Comercial:** **Hierarquia clara** quando o detector for crescer (mais bancos): primeiro emissor (banco), depois produto/bandeira. Bandeira NUNCA é evidência primária — só usar quando nenhum nome de emissor identifica. Documentar como regra interna. Em escala, virar tabela ranqueada por "força do sinal" (CNPJ > nome próprio do banco > telefone de atendimento > produto/bandeira > heurística genérica) que o detector percorre em ordem. Em B2B, expor essa lógica em telemetria/dashboard ajuda contadores a entenderem por que uma fatura foi roteada pra um parser específico.

### 30. Diagnóstico em camadas — checar a cadeia inteira antes de implementar correção
**Hoje:** Durante o debug da Fatia 3.2, gastei 2 patches (v0.12.1 e v0.12.2) implementando robustez no PARSER quando o bug estava no DETECTOR. Cada hipótese (pdfItems vazio → quirk de Y) era plausível e os testes locais confirmavam; mas em produção o parser nem era chamado. A pista que teria me poupado: o `__faturaDebug` veio "indisponível" — esse é um sinal forte de "este parser não rodou", não "este parser rodou mas falhou". Eu interpretei como "o objeto __faturaDebug não foi populado por algum motivo" (errado).
**Comercial:** Em escala, pipelines de importação têm 5+ etapas (upload → extração → detecção → parsing → revisão → confirmação → save). Cada etapa deve ter **debug observável separado**: "etapa X rodou, retornou Y". Sinal "indisponível" numa etapa específica deveria significar inequivocamente "essa etapa não rodou", e fazer pensar em "por que não chegou aqui?" — não "por que falhou aqui?". Em produção (B2B especialmente), telemetria deve capturar **breadcrumbs por etapa** com tempo, input/output, e razão de skip se aplicável. Acelera debugging em ordem de magnitude.

### 31. Layout colunar com ordem invertida no texto agrupado por Y — BTG vs Sicoob
**Hoje:** BTG e Sicoob têm a MESMA estrutura visual (3-4 colunas com X fixos: DATA | DESC | [CIDADE] | VALOR). Mas quando o PDF.js agrupa items por Y±2, o texto resultante sai em ordens diferentes: Sicoob mantém ordem visual ("DD ABR descrição cidade R$ valor"), BTG sai invertido ("R$ valor descrição DD Abr"). Razão: ordem dos items no content stream do PDF varia por gerador — quem o BTG usou produz items na ordem (X-direita, X-meio, X-esquerda) por linha, enquanto o Sicoob produz (X-esquerda, X-meio, X-direita). O modo A (com coordenadas X/Y) é imune porque usa âncora-de-DATA na coluna real X. O modo B (texto puro) precisa de regex específico pra cada ordem — implementei dois regexes no parserBTG (RX_INV pra BTG, RX_VIS pra padrão visual) e tento ambos em cada linha.
**Comercial:** Em escala, "ordem em que items aparecem no content stream do PDF" é decidida pelo software que gerou o PDF (não pelo PDF.js). Bancos terceirizam geração de fatura — pode mudar entre fornecedores ou versões. Pattern: **modo A com X/Y é a fonte da verdade**, modo B é fallback que precisa ser tolerante a ambas as ordens. Em produção, telemetria de "qual regex casou no modo B" ajuda a detectar geradores novos antes que quebrem.

### 32. Duplicação crescente de código entre parsers de banco — pendente consolidação
**Hoje:** Após Fatia 3.1 (Itaú Visa Infinite), 3.2 (Sicoob) e 3.3 (BTG), o app tem 3 parsers especializados + 1 genérico. Há duplicação significativa entre parserSicoob e parserBTG: parseValor robusto BR/US, extrairParcela em qualquer posição, montagem do objeto lanc, decisão best-of-A-or-B, escrita em window.__faturaDebug. Diferem só em: regex de mês (CASE vs Case), X-limites das colunas, filtros de descrição. Não consolidei agora pra não introduzir risco de regressão durante Fatia 3.3 (decisão consciente: estabilizar, depois refatorar). Está claro que com 4-5 bancos isso vira manutenção exponencial.
**Comercial:** **Refator pendente: `parserFaturaColunar(textoBruto, pdfItems, config)`** onde `config` define `{regexMesAncora, mesesMap, xCols: {data, desc, cidade?, valor}, filtroDesc, tratamentos especiais}`. Cada banco vira um objeto de config curto (10-15 linhas) em vez de função inteira (~150 linhas). Custo: 1 fatia técnica dedicada, sem nova feature visível. Benefício: adicionar um novo banco passa de "implementar parser inteiro" pra "fornecer config". Em B2B onde "suporte ao banco X" é frequente pedido de cliente, isso vira slope de entrega — diferença entre 1 dia e 4 horas de trabalho por banco.

### 33. Materialização + reconciliação: pattern híbrido pra projeção de gastos parcelados
**Hoje:** Fatia 4 ("Compromissos do mês") materializa parcelas faltantes ao importar uma compra parcelada (ex: IMUNOCENTRO 01/05 importada em MAI → cria 4 projeções 02/05, 03/05, 04/05, 05/05 nos meses seguintes com `isParcelaProjetada=true, pago=false`). Quando a fatura do mês seguinte chega com a parcela real, em vez de criar gasto novo, o app **reconcilia**: encontra a projeção compatível e ATUALIZA seus campos com dados reais (valor, data, categoria, marca pago=true, retira flag de projetada). Resultado: usuário vê os compromissos futuros antes mesmo das próximas faturas chegarem; não há duplicação. Pattern não-trivial — alternativas mais simples (só projeção visual, ou só materialização sem reconciliação) têm trade-offs piores: projeção pura não permite marcar pago manual; materialização pura duplica quando fatura nova é importada.
**Comercial:** Em escala, esse pattern habilita features de **planejamento financeiro real** (compromissos vs disponível, fluxo de caixa projetado, alertas de excedente) sem exigir double-entry bookkeeping. Risco: depende da chave de reconciliação ser estável (item 34). Em B2B, vira diferencial vs apps brasileiros que ou (a) só mostram a parcela do mês atual (sem visão futura) ou (b) duplicam gastos quando fatura nova chega. Refletir na UI: a tag "proj" explícita evita o pior cenário (usuário acha que já pagou algo que ainda é projeção).

### 34. Chave de reconciliação estável: `cardId | total | descricaoBruta normalizada`
**Hoje:** Pra reconciliar uma parcela importada de fatura nova com uma projeção pré-existente, o app gera uma chave determinística: `${cardId}|${parcela.total}|${descNormalizada}`. A `descNormalizada` é uppercase + remove acentos + remove não-alfanuméricos + slice(0,40). Critério-chave: usa `g.descricaoBruta` (texto ORIGINAL do PDF) em vez de `g.nome` (que o usuário pode ter editado). Razão: mesmo se o usuário renomear "IMUNOCENTRO SERV MED" pra "Plano Vacinas Família" no MAI, em JUN o PDF traz "IMUNOCENTRO SERV MED" → chave bate, reconcilia. Se usasse `g.nome` editado, perderia o match.
**Comercial:** Pattern reutilizável pra qualquer reconciliação de dados externos com edições do usuário: **preservar SEMPRE a fonte original** (`descricaoBruta`) separada da apresentação (`nome`). Em B2B/contabilidade, isso vira requisito de auditoria: "o que o sistema viu" vs "o que o usuário classificou" são campos distintos e ambos precisam ser preservados. Em escala, pode evoluir pra chave por fingerprint (descrição + valor exato + dia da compra) — mais robusta a colisões — mas com complexidade extra.

### 35. `state.schemaVersion` como marcador de migração one-time
**Hoje:** Fatia 4 introduziu 2 campos novos opcionais (`g.parcelaChave`, `g.isParcelaProjetada`, `g.isFixoProjetado`) sem migração disruptiva. Dados antigos continuam funcionando (campos undefined). Mas pra dados existentes terem o benefício novo (parcelas projetadas), a migração one-time `migrarParcelasV014()` roda automaticamente no boot, gerando `parcelaChave` em gastos existentes e materializando parcelas faltantes. Idempotente: marca `state.schemaVersion = '0.14'` quando termina; re-execuções skipam. Vantagem: zero coordenação manual ("rode esse script depois do deploy"); usuário só abre o app e está migrado.
**Comercial:** Pattern correto pra evolução de schema em apps client-side onde não há controle central de DB. Em escala: matriz de migrations `0.14 → 0.15 → 0.16 ...`, cada uma idempotente, rodadas em sequência baseado em `state.schemaVersion`. Equivalente a Rails migrations / Django migrations mas client-side. Em B2B com múltiplos clientes/contas, evita "1000 usuários em versões diferentes do schema". Risco a documentar: migrations precisam ser **defensivas** (gracefully handle dados malformados) — se uma migration crashar, o app inteiro fica preso.

### 36. Sincronização CRUD com projeções materializadas
**Hoje:** v0.14.1 adicionou o "compromisso de coerência" das projeções: quando o usuário pausa, edita ou remove um fixedExpense, as projeções futuras precisam refletir essa mudança imediatamente, MAS sem tocar nos gastos que já foram pagos (preservação de histórico). Implementado com duas funções: `limparProjecoesFuturasDoFixo(fixoId)` (filtra `parentFixoId === fixoId && pago === false && ym > corrente`) e `atualizarProjecoesFuturasDoFixo(fixoId)` (mesmo critério, mas atualiza campos em vez de remover). Cobertura: 5 cenários testados — pausar Netflix limpa 11 futuras mas mantém a paga; reativar recria; editar valor 30→45 atualiza só as futuras pendentes; remover limpa preservando histórico pago. **v0.14.2 estendeu o pattern pra gastos-parcela**: `limparProjecoesFuturasDeParcela(parcelaChave)` segue o mesmo critério (mesma chave + pago=false + ym>corrente). Disparada no handler de delete de gasto. UI: askConfirm avisa "esta compra parcelada tem N parcelas projetadas — também serão removidas" se houver projeções futuras pendentes. Toast pós-delete mostra o N. Cobertura adicional: 4 cenários testados.
**Comercial:** **Regra de ouro**: gastos pagos são FATOS — não devem ser alterados por mudanças no template (fixedExpense) ou por deleção de outra parcela. Gastos projetados são PLANOS — devem refletir mudanças no template e mudanças na compra parcelada. Critério único de discriminação: `g.pago === false && g.ym > currentYM()`. Pattern aplicável a qualquer sistema com "instanciação de templates" (recorrências, agendamentos, faturamento recorrente em SaaS B2B).

### 37. Saída técnica vinculada — pattern de "pagamento de fatura" como gasto reusando schema
**Hoje:** Etapa 4 (v0.15.0) fecha o ciclo cartão → conta: ao marcar fatura paga (manual ou automático via débito automático), o app cria um **"gasto técnico"** com `isPagamentoFatura: true`, `cardId: null` (pra não entrar no cálculo da própria fatura — evita loop), `contaId: card.contaPagamentoId` (debita a conta certa), `faturaCardId` + `faturaYm` (FK reversa pra recalcular). Vantagens do reuso de schema vs estrutura separada (`state.pagamentosFatura`): (a) `calcSaldoConta` já trata corretamente via filtro `pagamento !== 'cartao'`; (b) aparece no extrato da conta sem código novo; (c) save/load no Firestore funciona naturalmente; (d) renderização especial no extrato é simples (detectar a flag). Recalculo automático: ao adicionar/editar/remover gasto na fatura paga, `recalcularPagamentoFatura(cardId, ym)` ajusta o valor da saída técnica (ou remove se fatura ficar vazia). Boot: `processarPagamentosFaturaAutomaticos()` varre cartões com `debitoAutomatico=true` e cria pagamentos pra faturas vencidas (idempotente). Validação: 9/9 cenários.
**Comercial:** Pattern reutilizável: **"saída técnica vinculada"** pode modelar transferências espelho entre contas, ajustes com snapshot histórico, devoluções/estornos. Princípio: reusar o objeto-base do domínio (gasto/transação) com flags específicas + FK reversa permite que toda a infra existente (renderização, totais, saldos, sync) funcione sem código novo. Custo: ~10 linhas de código pra cada pattern novo. Em B2B (contabilidade, conciliação automática), esse pattern é a base da implementação. Atenção: `cardId: null` no pagamento técnico é crítico (sem isso entra em loop infinito no `calcularTotalFatura` — recálculo gerando recálculo). Princípio derivado: **fontes de cálculo devem ser separáveis das saídas do cálculo**.

### 38. SVG inline vs Chart.js para gráficos: decisão por especificidade visual
**Hoje:** Fatia 5.1 dos Relatórios (v0.16.0) introduziu 4 gráficos (total consolidado, por categoria com subcat empilhada, multi-mês com 2 curvas sobrepostas, small multiples 2x2). Apesar do Chart.js já estar carregado via CDN no head, optou-se por **SVG inline manual** em todos os 4. Razões: (a) controle pixel-perfect das duas linhas finas e discretas sobrepostas no mesmo viewBox (mediana tracejada `stroke-width=1` + média móvel suavizada `stroke-width=1.25` via Catmull-Rom→Bezier com tensão 0.18); (b) tooltip nativo via `title` attribute em vez de plugin do Chart.js; (c) cores de subcategoria derivadas em runtime via mix com branco (`_relMixWhite`, ratio 0..0.6) — abordagem inviável em Chart.js sem pré-calcular paleta; (d) zero JS de runtime extra no first paint (todo SVG sai pronto do render server-side equivalente); (e) drill-down via `data-rel-subcat` attribute resolve diretamente em event handler — sem custos do hit-test do Chart.js. Chart.js fica no head pra fatias futuras com gráficos mais complexos (pizza com tooltips ricos, mixed line+bar com legenda interativa).
**Comercial:** Princípio: **bibliotecas de chart genéricas existem pra resolver problemas genéricos**. Quando o requisito visual é específico (mediana + tendência sobrepostas + cores derivadas matematicamente + interação em segmento individual), o custo de domar a lib supera o de escrever SVG direto. Em B2B onde "exportação fiel pra PDF/print" e "tema customizável por cliente" são frequentes, SVG inline também é mais robusto (compatibilidade universal, fontes inheritáveis, vetorial). Risco a documentar: SVG inline não escala automaticamente em containers fluidos sem `viewBox` + `preserveAspectRatio` configurados corretamente — bug clássico que mata a UI mobile.

### 39. Hero sempre mostra o mês corrente, mesmo em modo multi-mês
**Hoje:** Fatia 5.1 dos Relatórios: o card "hero" no topo (total grande + pago + previsto + % comprometido em fixos) **sempre reflete o mês corrente selecionado**, mesmo quando o usuário seleciona "últimos 6 meses" no seletor de período. O seletor afeta apenas as seções abaixo (gráfico multi-mês, small multiples, lentes de grupos/nomes/identificações). Razão: confusão cognitiva. Se o hero virasse "média mensal" ou "total dos 6 meses", o usuário precisaria interpretar dois números no mesmo card (`R$ 50.100` "isso é o mês ou o período?"). Mantendo hero fixo no mês = âncora temporal estável, lentes abaixo = comparação histórica.
**Comercial:** Princípio aplicável a qualquer dashboard com seletor de período: **o número-âncora deve ter granularidade constante**. A informação adicional do período seleciona o que MUDA visualmente (gráficos, lentes), não o que serve de referência. Em B2B (analytics, BI), esse princípio reduz o "what does this number mean?" — disparador comum de tickets de suporte e UX mal-resolvido.

### 40. Lentes de relatório como seções empilhadas, não tabs
**Hoje:** Fatia 5.1: as 4 lentes de agrupamento do Relatório 1 (subcat, grupos cross-cutting, top nomes, top identificações) entram como **seções verticalmente empilhadas** num único scroll, não como tabs. Razão: tabs forçam escolha mutuamente exclusiva ("ver por subcat OU por grupo"), enquanto seções empilhadas convidam a comparação lateral ("o gasto em 'Plano Saúde' como subcat de Casa, também aparece no grupo 'Saúde Família' agregando Marina+Heitor"). O custo extra de scroll é tolerável em mobile pra um relatório que o usuário consulta com atenção (1-2 vezes por mês), não em fluxo rápido. Tabs ficariam melhores em views de consulta diária (entradas, contas), não relatórios analíticos.
**Comercial:** Princípio: **tabs escondem; seções empilhadas convidam comparação**. Em B2C de finanças pessoais, a história "uau, esse gasto aparece em 3 lentes diferentes" gera o momento de insight que vira retenção. Em B2B de relatórios, dashboards com lentes empilhadas tendem a ter NPS maior — mais informação visível sem clicks adicionais. Trade-off: scroll longo. Mitigação: seção de título com eyebrow forte (uppercase pequeno + ícone) cria âncoras visuais de scroll.

### 41. Visão econômica vs financeira: mesma base, lentes diferentes
**Hoje:** Fatia 5.2 (Relatório 2 — Fluxo de Caixa, v0.17.0) introduziu a **visão financeira** ao app: cartão de crédito não conta como saída no mês da compra; só conta quando a fatura é paga (`isPagamentoFatura: true`). Já o Relatório 1 (Fatia 5.1) usa **visão econômica**: cada compra de cartão conta no mês da compra, e pagamento de fatura é *excluído* do relatório (caso contrário duplicaria o consumo). Mesma base de dados (`state.gastos`), filtros opostos no runtime: `_relGastoConta` (econômica, exclui `isPagamentoFatura`) vs `_rel2FluxoConta` (financeira, exclui `pagamento === 'cartao'` mas inclui `isPagamentoFatura` que tem `pagamento: 'conta'`). O usuário escolhe a lente trocando de relatório via pills, não trocando de filtro dentro do mesmo relatório.
**Comercial:** Pattern aplicável a qualquer sistema com múltiplas perspectivas sobre os mesmos fatos: **schema único, lentes múltiplas via funções puras de runtime**. Em B2B de contabilidade isso é fundamental — regime de caixa vs regime de competência (accrual basis) é exatamente essa dicotomia, e cada empresa precisa dos dois pra fechamento fiscal e gestão financeira respectivamente. Em fintechs, "cartão como compra" vs "cartão como crédito" também é a mesma estrutura. Princípio derivado: **se duas visões disputam o mesmo render, separe a UI; se compartilham infra mas precisam de filtros opostos, mantenha o filtro como parâmetro explícito do cálculo, não como branch no render**. Custo de carregar 2 relatórios separados (5.1 e 5.2): zero esforço pra fatia 5.3 e além — só adicionar pill + sub-render.

### 42. Dispatcher com pills resolve "vista com sub-vistas"
**Hoje:** Antes da Fatia 5.2, `renderRelatorios()` era um render monolítico do Relatório 1. Pra adicionar o Relatório 2 (e os futuros 5.3, 5.4) refatorou-se em **dispatcher leve**: renderiza pills no topo + delega pra sub-renders (`_renderRel1Inner()` e `_renderRel2Inner()`). Cada sub-vista tem estado próprio (`relState`, `rel2State`) que é preservado ao alternar — usuário pode trocar entre Rel 1 e Rel 2 sem perder ym/periodo/toggle de cada um. Anti-pattern alternativo (rejeitado): cobrir tudo num único render gigante com condicionais (`if (currentReport === 'rel1') ... else ...`). Em 3-4 fatias isso vira inmanutenível.
**Comercial:** Pattern aplicável a qualquer view que ganha múltiplos modos depois da implementação inicial: refatorar pro **dispatcher + sub-renders independentes** custa ~30 linhas e paga dividendos imediatos. Em B2B de SaaS isso é o padrão "tabs com state preservado" — esperado pelo usuário. Princípio: **estado independente por sub-vista é o que faz tabs serem úteis em vez de irritantes**. Se ao trocar de tab tudo reseta, o usuário precisa reconfigurar — atrito que mata o uso da feature.

### 43. Schema asymmetry como microcopy honesta
**Hoje:** Fatia 5.2 expôs uma assimetria do schema atual: `state.gastos` tem `categoria` (member-bound: 'casa', 'marina', ..., 'tiago'), mas `state.entradas` tem só `{id, fonteId, contaId, valor, data, ym}` — sem campo de membro. Implicação: o toggle "Incluir Tiago no fluxo da família" não pode afetar entradas simetricamente. Decisão: em vez de fingir simetria ou inventar lógica frágil ("considera entradas via conta Tiago"? "considera entradas com observação contendo 'tiago'"?), escrever **microcopy explícita**: quando o toggle está ON, aparece "*entradas: família · saídas + invest: família + Tiago*". Quando OFF, "*entradas e saídas: apenas família*".
**Comercial:** Princípio: **quando o modelo de dados tem cantos não-ortogonais, explicitar a assimetria via texto é mais robusto do que tentar resolver visualmente**. Usuário informado é usuário confiante; usuário enganado por simetria fake é usuário que perde confiança ao descobrir. Em B2B (financeiro, contabilidade), microcopy honesta sobre o que está sendo calculado é diferencial — bons relatórios financeiros sempre têm rodapé técnico ("inclui/exclui X, Y, Z"). Refator de schema (adicionar `fonteOwner` nas entradas?) fica como decisão consciente futura, não dívida silenciosa. NB pessoal pro Tiago: se algum dia a Juliana entrar no app com renda própria registrada (Etapa B futura), aí o `fonteOwner` vira necessário. Hoje, simplificação assumida.

### 44. Coerência visual: "tudo colunas, nada linhas" como princípio estético
**Hoje:** No primeiro draft da Fatia 5.2 (v0.17.0), o multi-mês do Rel 2 usava barras agrupadas (E·S·I) com **linha tracejada Bezier de saldo sobreposta** — mistura de barras + linha que parece poderosa em teoria mas vira ruidosa visualmente em mobile pequeno (linha luta com as barras pela atenção). Nos ajustes da v0.18.0, a linha foi substituída por uma **4ª coluna divergente** por mês: pra cima se saldo positivo (verde), pra baixo se saldo negativo (vermelho), com eixo zero como linha cinza sutil. Resultado: gráfico mais legível com mesmo conteúdo informacional. Trade-off documentado: comparações de evolução temporal são mais difíceis sem linha (olho perdeu a "trajetória"), mas o ganho de clareza local supera em multi-mês curto (3-6 meses).
**Comercial:** Princípio: **dentro de um mesmo relatório, escolher um vocabulário visual e mantê-lo**. Misturar barras + linhas + areas no mesmo gráfico mobile não funciona — desktop tolera mais, mobile pune. Em dashboards B2B, "consistência > variedade" é regra de ouro pra retenção de atenção. Em mobile específico: prefira sempre colunas pra valores discretos (mês, categoria, item) e reserve linhas pra séries contínuas longas (>12 pontos) onde a tendência é a história. Lição prática: o primeiro draft visual quase sempre tenta "fazer demais" — o segundo draft simplifica e fica melhor.

### 45. Subcategorias consolidadas como lente complementar de "consumo total"
**Hoje:** Os ajustes da v0.18.0 adicionaram no Rel 1 uma seção nova: **"consumo · por subcategoria (top 10)"** — agrega todas as subcategorias somando todas as categorias incluídas (ex: Moradia da Casa + Marina + Heitor + Juliana → R$ X). Visualmente colunas verticais finas em scroll horizontal, cor única terracota (`--flow-out`), valor total acima em Fraunces, nome embaixo. Drill-down: lista todos os gastos daquela subcat em qualquer categoria do período. Complementa o gráfico "por categoria · subcategorias" (que mostra subcat × categoria) — esse novo mostra apenas subcategoria. Decisão de tomar a tabela cat × subcat (2D) e projetar em apenas 1 das 2 dimensões — porque na prática só uma das duas dimensões geralmente importa de cada vez.
**Comercial:** Princípio: **dados multidimensionais merecem múltiplas projeções, não uma matriz gigante**. Em B2C de finanças pessoais, a pergunta "quanto gastei com transporte no total" é muito comum, mas a estrutura cat × subcat força a resposta "transporte da Marina + transporte do Heitor + ...". Lente consolidada resolve isso com uma única coluna — UX que reduz o trabalho cognitivo do usuário. Em B2B (analytics), oferecer pivot dinâmico via dropdown ("agrupar por: categoria | subcategoria | conta | etc") é o equivalente. Custo da lente nova: ~50 linhas de código (reusa toda a infra de drill-down).

### 46. Escala honesta vs escala adaptativa em gráficos com magnitudes diferentes
**Hoje:** A seção "composição do mês" no Rel 2 (v0.18.0) mostra 3 colunas: Entradas (R$ 9k), Saídas (R$ 7k), Saldo (R$ 2k). Decisão de design: **mesma escala vertical pras 3 colunas**. Consequência: a coluna de saldo aparece como ~22% da altura de entradas, comunicando visualmente "saldo é uma pequena fração do fluxo total". Alternativa rejeitada: escala adaptativa por coluna (cada uma usa 100% da altura disponível) — daria visualmente "saldo grande" mesmo quando R$ 2k é proporcionalmente pequeno frente a R$ 9k. Saldo positivo aparece coluna pra cima em verde; negativo, pra baixo em vermelho.
**Comercial:** Princípio: **gráficos devem comunicar magnitude relativa de forma honesta, mesmo quando isso torna alguns elementos pequenos**. Em B2B (relatórios financeiros), escalas adaptativas são fonte de confusão e desconfiança — um saldo de R$ 2k aparecer "grande" porque a escala foi ajustada faz o usuário sub-estimar a importância de R$ 7k de saídas. Em B2C (apps de finanças), o mesmo: "meu saldo está enorme" é uma ilusão que vira surpresa negativa no fim do mês. Trade-off: legibilidade local (coluna pequena pode ter texto cortado) vs honestidade global (proporções fiéis). Mitigação: valor em texto sempre visível acima da coluna (em Fraunces), garantindo que mesmo a barra mais fina comunique seu valor exato.

### 47. Parametrização preventiva paga dividendos compostos
**Hoje:** Na Fatia 5.1 (v0.16.0), todas as funções de cálculo (`relCalcHero`, `relCalcTotalConsolidado`, `relCalcGrupos`, `relCalcTopNomes`, etc.) foram escritas aceitando `cats` (array de categorias) como parâmetro, mesmo que naquela fatia o array sempre vinha de `relCategoriasIncluidas()`. Custo extra na hora: ~0 linhas — apenas mudar `g.categoria === fixedKey` por `cats.indexOf(g.categoria) >= 0` no filtro. Benefício acumulado: na Fatia 5.2 (Rel 2) essas funções já receberam um cats diferente (categorias de saída); na Fatia 5.3 (Rel 3 = Meus Gastos · Tiago, v0.19.0), 80% dos cálculos foram reusados literalmente com `['tiago']` ou `['casa', 'marina', 'heitor', 'juliana']` sem nenhum código novo. A Fatia 5.3 inteira saiu em ~611 linhas (vs 1.077 da 5.1) — quase 50% de redução de esforço, com mais funcionalidade (toggle comparativo).
**Comercial:** Princípio: **se uma função vai ser usada em mais de 1 contexto previsivelmente, parametrize o contexto desde a primeira chamada**, mesmo que pareça "premature parametrization". O ROI vem composto: cada nova fatia paga uma fração do investimento original, mas o investimento original foi quase zero. Em B2B (SaaS de finanças/relatórios), esse pattern é diferencial competitivo — features novas saem rápido porque a infra já estava pronta. Anti-pattern oposto: hardcoding por suposição de "YAGNI estrita" (you aren't gonna need it) — funciona pra coisas isoladas, mas não pra módulos com claro potencial de variação. Heurística: **se você consegue imaginar 2+ contextos onde a função pode ser usada, parametrize**. Se imagina 1 só, não parametrize.

### 48. Toggle muda enquadramento, não disponibilidade
**Hoje:** No Rel 3 (Meus Gastos · Tiago, v0.19.0), o toggle "Comparar com a família" não esconde nem revela informação — ele troca a perspectiva sobre os mesmos dados. Quando OFF: hero focado no Tiago com cor verde, multi-mês com barras stacked + curvas (mediana + média móvel). Quando ON: hero com 2 colunas (Tiago + Família, %), multi-mês com barras agrupadas Tiago vs Família. Anti-pattern rejeitado: toggle "mostrar gráfico comparativo: sim/não" — vira interruptor binário que parece útil mas atrapalha o fluxo de análise (o usuário acaba precisando ligar/desligar várias vezes pra navegar). Pattern correto: toggle como **dimensão analítica** — solo vs comparativo são duas perspectivas válidas do mesmo dado, escolhidas conforme a pergunta sendo feita ("quanto gastei?" vs "como me comparo?").
**Comercial:** Princípio: **em UI analítica, toggles devem reformular a apresentação, não controlar visibilidade**. Em B2B (BI, analytics), dashboards bons usam toggles pra alternar entre "valores absolutos" e "% do total", entre "diário" e "acumulado", entre "valor" e "variação vs período anterior" — sempre mostrando os mesmos KPIs sob lentes diferentes. Em B2C (apps de finanças/saúde), o mesmo padrão: "meta diária" vs "média semanal", "saldo atual" vs "projeção fim do mês". Princípio derivado: **toggle que esconde informação obriga o usuário a memorizar o estado anterior pra comparar — quebra a análise**. Toggle que reformula deixa a comparação no próprio gesto de alternar.

### 49. Estruturas paralelas não-sincronizadas = bug latente esperando manifestar
**Hoje:** O bug do HANSARD (v0.19.1) veio de duas estruturas que representavam "a mesma verdade" mas evoluíram separadamente: `state.investMods` (modalidades com UI completa em Config) e `state.subcatsInvestimento` (lista consultada pelo filtro `isInvest()`, sem UI, com default fixo). O usuário cadastrava modalidades pela UI esperando que isso "ativasse o reconhecimento", mas o filtro nunca lia essa lista. Resultado: HANSARD ficava com `g.isInvest = false` na criação e o app nunca o reconhecia retroativamente. Correção foi tripla: (1) `isInvest()` consulta as duas listas (consolidação no leitor), (2) `_isInvestRuntime(g)` aplicado em 17 lugares ao invés de `g.isInvest` direto (corrige retroativo sem migração obrigatória), (3) migration silenciosa idempotente no boot pra reconciliar gastos antigos.
**Comercial:** Princípio: **toda vez que existirem duas estruturas representando a mesma verdade, é questão de tempo até dessincronizarem**. Em B2B (SaaS), isso é fonte clássica de bugs em integrações (CRM + email marketing, sistema fiscal + ERP, contabilidade + bancos). Soluções: (1) ponto único de consulta com fan-out (um "leitor universal" como `_isInvestRuntime`), (2) sincronização event-driven com idempotência (mudou A, atualiza B automaticamente), (3) migration silenciosa idempotente no boot. Lição prática derivada: **se você criou uma UI pra editar uma estrutura, garanta que o filtro/leitor consulta essa estrutura — nunca uma "lista paralela" hardcoded ou com default fixo**. Em produtos vendidos, isso é diferencial: bugs de sincronização são extremamente difíceis de reproduzir pelo cliente e geram fricção crônica.

### 50. Regimes contábeis (econômico vs financeiro) precisam de microcopy constante
**Hoje:** O Tiago questionou repetidamente "como gastei R$ 56k mas saiu só R$ 43k da minha conta?" — confusão entre regime de competência (Rel 1: gasto conta no mês em que aconteceu) e regime de caixa (Rel 2: só conta o que saiu da conta no mês). Tecnicamente: cartão comprado em maio fica no Rel 1 de maio, mas só aparece no Rel 2 quando a fatura é paga (junho). Mesmo entendendo intelectualmente uma vez, o usuário esquece — porque na vida normal "gasto" e "saída de dinheiro" coincidem. Solução implementada: microcopy em itálico discreto **em cada hero** dos relatórios. Rel 1: "regime econômico: gastos contam no mês em que aconteceram". Rel 2: "regime de caixa: compras de cartão entram no mês em que a fatura é paga. Faturas não pagas ainda não saíram." Recorrente, em todo render, sem precisar abrir FAQ.
**Comercial:** Princípio: **diferenças conceituais sutis precisam de lembrete CONSTANTE no contexto onde aparecem, não numa documentação separada**. Em B2B (contabilidade, finanças, BI), termos técnicos precisam de tooltips e microcopy *in situ* — não num glossário. Em fintechs B2C ("rendeu X% no mês mas tô vendo Y% no extrato — diferença?"), a comunicação clara de regime é o que separa apps que confundem usuários dos que educam. Diferencial concreto: cada KPI tem 1 linha explicativa sob ele, em itálico, com peso visual reduzido — visível mas não-intrusiva. **Microcopy bem feita é UX educativa contínua — diferente de onboarding pontual que o usuário esquece**.

### 51. Hierarquia de fallback escala melhor que dimensões paralelas
**Hoje:** Pra "tipos de investimento" (v0.21.0), o Tiago tinha 2 opções: criar uma subcategoria por tipo (HANSARD, CDB Itaú, FII XPLG...) — vira poluição cadastral quando ele só faz 1 aporte por mês em cada — OU usar campos genéricos (identificação, grupo) pra diferenciar. Solução escolhida: **hierarquia de fallback** — `_relInvestChaveTipo(g)` retorna agrupamento por `identificacao` se preenchida, senão por nome do `grupo`, senão por `subcategoria`. Auto-degradação elegante: mesmo sem ninguém preencher nada extra, aparece agrupado pela subcat. Quando preenche identificação, granularidade aumenta. Quando preenche grupo, vira intermediário. Tag visual (`•` idf, `🔗` grupo, `—` subcat) indica origem do agrupamento — usuário entende como sua escolha está sendo lida.
**Comercial:** Princípio: **quando o usuário tem N formas de preencher a mesma informação (sinônimos no schema), defina uma hierarquia explícita e mostre visualmente qual está em uso**. Anti-pattern: forçar uma forma única (rígido) ou tratar todas como independentes (sobreposição confusa). Pattern correto: hierarquia + transparência visual. Em B2B (CRM, helpdesk), isso aparece como "preencha categoria OU tag OU ambos — o sistema agrupa pela mais específica que existir". Em fintechs B2C: "classifique a transação OU categorize OU adicione comentário — relatórios usam o que estiver mais detalhado". Lição operacional pra produtos: oferecer múltiplas formas de preencher tem custo zero de UX **se houver hierarquia clara e indicador visual**. Sem isso, usuário fica indeciso ("uso qual?") e dados ficam inconsistentes.

### 52. Refactor sistêmico requer enumeração explícita de callers
**Hoje:** Na v0.20.0, modifiquei o filtro `_relGastoConta` pra criar `_relGastoSaida` (inclui invest no total). Apliquei nos relatórios mas esqueci do módulo Família/Tiago/Config, que usa outra função (`totaisCategoriaMes`). Tiago descobriu na v0.20.2: "os investimentos continuam fora da soma total de gastos da família no módulo 3". Fix exigiu nova rodada (estender `totaisCategoriaMes` retornando invest, atualizar 3 callers). 30 segundos de `grep totaisCategoriaMes` no início teriam economizado uma versão inteira.
**Comercial:** Princípio: **antes de modificar qualquer função "central" (chamada de mais de 1 lugar), enumere os callers explicitamente e raciocine sobre cada um**. Em B2B (SaaS com módulos), isso é diferença entre updates seamless e updates que quebram features esquecidas. Em codebases médios/grandes, ferramentas estáticas (grep, IDE find usages, dependency graph) tornam isso barato — mas a disciplina de USAR essas ferramentas antes do refactor é o que separa engenheiros sêniores de juniors. Lição operacional pra agentes que escrevem código (humano ou IA): **antes de mudar API de função compartilhada, listar os callers + raciocinar sobre cada um na resposta**, mesmo que pareça "óbvio que só afeta X".

### 53. Cor como informação, não como decoração
**Hoje:** A v0.21.2 substituiu a cor única terracota das 10 colunas do gráfico "consumo · por subcategoria" pela cor da **categoria predominante** em cada subcategoria (membro com maior contribuição naquela linha). Implementação: campo `porCat: { catKey: valor }` adicionado em `relCalcSubcatsConsolidadas`, helper `_relSubcatCatDominante(sc)` retorna o key da categoria com maior valor, renderer usa `relCatColor(catDom)` em vez de `var(--flow-out)` fixo. Resultado visual: olhando o gráfico agora, fica imediatamente claro que "Moradia" mora majoritariamente na Casa, "Esporte" no Heitor, etc. — informação que antes precisava de drill-down pra descobrir. Antes da mudança, considerei opção alternativa (paleta categórica fixa de 10 tons distintos) e rejeitei: cores diferentes só pra "ficar bonito" são decoração, não informação. Cores diferentes derivadas de uma propriedade real dos dados são informação.
**Comercial:** Princípio aplicável a qualquer dashboard analítico: **antes de escolher uma paleta, perguntar "essa cor codifica algo real ou é arbitrária?"**. Cor arbitrária é ruído visual — usuário não consegue ignorar porque não sabe que é arbitrária; gasta atenção tentando decodificar. Cor que codifica uma dimensão real dos dados (categoria predominante, tendência, severidade, tier de cliente) é amplificador de leitura — usuário decodifica uma vez e ganha sinal contínuo. Em B2B (BI/analytics), isso é a diferença entre dashboard que "comunica" e dashboard que "polui". Implementação tem custo baixo (poucas linhas) se a função de cálculo já estiver retornando dados granulares — basta agregar uma dimensão a mais (`porCat` no caso). Anti-pattern relacionado: hardcoded palette de N cores aplicada por ordem do array — comunica nada além da posição, que muitas vezes é arbitrária.

### 54. Uma pergunta financeira = uma resposta consistente, independente de onde for feita
**Hoje:** Na v0.22.2 auditei divergência entre a tela Família/Compromissos e o Rel 1/Compromissos do mês — ambas mostravam totais distintos pra mesma pergunta ("quanto está comprometido este mês?"). Família: R$ 41.402,39. Rel 1: R$ 38.817,04. Gap exato de R$ 2.585,35 = um investimento fixo HANSARD que o Rel 1 excluía via filtro `_isInvestRuntime`, mas que a tela Família incluía na soma de "Gastos fixos" (que conta tudo com flag `isFixo`). Reconstrução matemática confirmou que ambos os cálculos estavam matematicamente corretos individualmente — não era erro de soma, era **diferença de definição**. Fix removeu a exclusão de invest no Rel 1, unificando as duas vistas em R$ 41.402,39. Aceita-se sobreposição visual com a seção "investimentos · por modalidade" (mesmo gasto sob 2 lentes — compromisso vs categoria), microcopy esclarece.
**Comercial:** Princípio fundamental pra auditoria financeira: **uma métrica deve ter UMA definição canônica, mesmo que apresentada em múltiplos lugares**. Quando duas telas dão respostas diferentes pra mesma pergunta, é bug funcional independente de matemática estar correta dos dois lados — usuário perde confiança no sistema. Implementação prática: extrair um único helper canônico (ex: `calcularCompromissosCanonico(ym, view)`) chamado pelas telas que precisam do mesmo conceito; documentar o que cada métrica conta exatamente; testar invariantes ("Σ por categoria = total geral", "Rel A + Rel B = consolidado"). Em B2B (contabilidade, ERP, BI), esse princípio é a diferença entre sistema confiável e sistema "que dá um número diferente toda vez que você abre". Anti-pattern: cada tela calcular sua própria definição "porque parecia mais conveniente naquele contexto" — drift silencioso garantido. Em fintechs, regulamentações exigem reconciliação cruzada — sistemas que não respeitam esse princípio falham auditoria de compliance.

### 55. CSS @media print exige seletores reais do DOM, não suposições por nome comum
**Hoje:** Na v0.22.0 implementei exportação PDF via `window.print()` + `@media print`. Apostei em nomes de classe "comuns" sem verificar a estrutura real (`#viewRelatorios`, `.tab-bar`, `.tabbar`, `.nav`, `.header`). Resultado: PDF saiu **completamente em branco** — a regra `.view { display: none }` escondeu todas as views, mas o seletor `#viewRelatorios` que tentava re-mostrar a de relatórios não existia no DOM (a view real era `<section class="view" data-view="relatorios">`, sem id); a tab bar também não foi escondida porque era `.bottom-nav`, não `.tab-bar`. 30 segundos de `grep` no início teriam evitado a versão quebrada e o re-deploy.
**Comercial:** Princípio: **antes de escrever CSS de impressão (ou QUALQUER CSS que dependa de seletores específicos), inspecionar o DOM real e usar nomes que existem**. Apps single-file ou de uma empresa específica raramente seguem convenções globais — `.tab-bar` em um, `.bottom-nav` em outro, `.nav-bottom` em outro. Lição operacional pra LLMs escrevendo código: nunca apostar em "nome provável" quando você pode `grep` em 1 comando. Aplicação B2B (apps complexos, white-label): CSS de impressão é uma das primeiras coisas que clientes pedem e uma das mais frágeis — quebra silenciosamente porque ninguém testa impressão até precisar. Boas práticas: data-attributes dedicados (`data-print="hide"`), classes utilitárias específicas (`.no-print`), suite de testes visuais de impressão (Percy/Chromatic) se for produto sério. Outra lição: o CSS de impressão precisa esconder TUDO que não é o conteúdo principal (nav, FABs, sheets, toasts, modais, banners) — fácil esquecer um elemento e ele aparecer indevidamente no PDF.

### 56. CSS global `appearance: none` + `width: 100%` em `input` quebra checkboxes silenciosamente
**Hoje:** Na v0.22.2 corrigi bug pré-existente (não introduzido pela Fatia 5.5) que fazia todos os checkboxes do app aparecerem como "barras brancas grandes" em vez de checkboxes nativos. Causa: regra global `input, select, textarea { -webkit-appearance: none; appearance: none; background: var(--bg-elevated); width: 100%; padding: 12px 14px; ... }` — pattern comum em apps minimalistas pra estilizar inputs de forma consistente — cobre `<input type="checkbox">` também, transformando o checkbox numa div retangular branca de largura total. Bug existia desde sempre, mas Tiago só notou quando explorou tela específica com toggle visível. Confirmado via `diff` com versões anteriores que eu não tinha introduzido a regressão. Fix preventivo global: `input[type="checkbox"] { -webkit-appearance: auto !important; appearance: auto !important; width: auto !important; padding: 0 !important; background: transparent !important; border: none !important; box-shadow: none !important; }` — restaura aparência nativa em todos os checkboxes do app (toggles, forms de cartão, forms de gasto, import, bulk operations).
**Comercial:** Princípio: **resets agressivos de CSS precisam de exceções explícitas pros tipos que dependem de aparência nativa**. Inputs de tipo `checkbox`, `radio`, `range`, `color`, `file` têm semântica visual que `appearance: none` destrói — e como o reset usual também aplica `width: 100%` + `padding`, o elemento fica grotescamente errado. Pattern similar em Tailwind CSS, Material UI base, app de finanças, é-commerce — onde quer que reset universal de input apareça. Lição operacional: ao escrever ou herdar reset de CSS, listar explicitamente as exceções de tipos que devem manter `appearance: auto`. Em produtos B2B com forms complexos, esse tipo de bug compromete usabilidade silenciosamente (formulários "parecem ok" mas usuário não consegue distinguir checkbox marcado de desmarcado, radio button vira retângulo, etc). Antipattern relacionado: testar só com `<input type="text">` e assumir que o reset funciona — sempre testar a matriz de tipos.

### 57. window.print() + CSS @media print > jsPDF + html2canvas pra PDFs profissionais
**Hoje:** Na Fatia 5.5 (v0.22.0), revisitei a decisão do prompt mestre v15 que previa `jsPDF + html2canvas` pra exportação de relatórios. Análise das duas abordagens contra o objetivo "relatórios dignos de auditor fiscal profissional":

| Aspecto | `jsPDF + html2canvas` | `window.print()` + CSS print |
|---|---|---|
| Texto no PDF | Imagem rasterizada (não selecionável) | Vetorial (selecionável, pesquisável, copiável) |
| Tamanho | Vários MB típico | 50-200KB típico |
| Fontes customizadas | Pode quebrar/usar fallback | Renderizam corretamente (motor nativo) |
| Cores CSS variables | Bugs conhecidos em alguns navegadores | Funcionam normalmente |
| Confiabilidade iOS | Bugs históricos em Safari mobile | Sólido (10+ anos de uso) |
| Libs externas | jsPDF ~300KB + html2canvas ~150KB | Zero |
| UX | "1 clique, baixou" | "1 clique no app → 3 toques no iOS (Compartilhar → Imprimir → Salvar em Arquivos)" |
| Controle granular | Alto (API completa de jsPDF) | Médio (CSS print + counters) |

Conclusão: pro caso "auditor profissional", `window.print()` é claramente superior. Auditor quer buscar valores (`Cmd+F`), copiar números pra planilha, anexar em email/sistema, fazer OCR/extração automatizada — tudo só funciona com PDF de texto. Os 3 toques extras no iOS são tradeoff aceitável pela qualidade do output. Implementado bloco `.rel-print-meta` invisível em tela mas visível no PDF com identificação completa (título, período, data, usuário, versão).
**Comercial:** Princípio: **escolha de tecnologia deve servir o caso de uso real, não a aparência de "modernidade"**. `jsPDF + html2canvas` é "mais sofisticado tecnicamente" (lib JS dedicada, renderização programática), mas inferior pro objetivo. `window.print()` é "antigo" e tem UX menos slick, mas entrega PDF que serve auditores de verdade. Lição B2B: pra qualquer feature de exportação/relatório, perguntar primeiro "quem vai consumir esse PDF e como?" — auditor/contador/regulador quer texto buscável; usuário pra share rápido quer screenshot bonito; jurídico quer PDF/A; cada caso uma stack diferente. Anti-pattern: escolher tech pela popularidade no Stack Overflow sem mapear o caso de uso. Quando vale jsPDF: layouts altamente customizados (faturas com QR codes, certificados com fontes complexas, PDFs interativos), ou quando UX de "1 clique baixou" é crítica (consumer apps casuais). Quando vale window.print(): relatórios profissionais, dashboards corporativos, documentos pra auditoria/compliance.

### 58. Lote de importação como FK reversa habilita undo cirúrgico
**Hoje:** Na v0.23.0 (Etapa 5b inicial), importação de extrato escrevia direto em `state.gastos` e `state.entradas` sem tracking. Resultado: quando Tiago clicou "aplicar" sem revisar, ~50 gastos entraram como "Tiago > a classificar" e a única forma de desfazer era deletar manualmente um por um. **v0.23.1 introduziu o pattern de lote:** novo schema `state.lotesImportacao[]` com objetos `{id, tipo, contaId, dataImport, descricao, gastoIds[], entradaIds[], quantidade, isRecovery?}`; cada gasto/entrada importado carrega `loteImportacaoId`. Undo é `state.gastos.filter(g => !loteIdsSet.has(g.id))` + remover o lote do array. **Migração retroativa idempotente** roda no boot: gastos órfãos com `origem: 'extrato'` mas sem `loteImportacaoId` são agrupados num único lote de "🛟 Recuperação" automaticamente — Tiago não perde dados de versões anteriores, ganha undo retroativo de graça. Helper `desfazerLoteImportacao(loteId)` + UI no Config "📥 Histórico de importações" com confirmação modal.
**Comercial:** Pattern aplicável a QUALQUER operação em lote num app: importação de dados, criação em massa, deleção em massa, sync com fonte externa. **Princípio: toda escrita em massa cria um objeto-tracking que permite reverter**. Em B2B (ERP, CRM, contabilidade), isso é hard requirement — auditor exige rastreabilidade ("quem importou o quê, quando, e como reverter se errou"). FK reversa é o pattern certo: o lote referencia os IDs dos records criados; deleção do lote = deleção dos records. Migração retroativa mostra que dá pra introduzir tracking depois sem perder dados antigos — apenas marca-os como "lote de recuperação". Em escala, lotes viram entidade de primeira classe: histórico, busca, métricas ("X% das importações foram desfeitas → parser pode estar errado"), e até versionamento opt-in ("ver como estava o app antes deste lote").

### 59. Dedup em 3 níveis de confiança > exclusão binária silenciosa
**Hoje:** v0.23.0 fazia dedup binário (`existente ? excluir : incluir`), com query rígida (`g.contaId === contaId` + `±R$ 0,01` + `±2 dias`). Resultado: Eleva (R$ 6.283,27) e Wise/Hansard (R$ 2.585,35) — gastos já cadastrados manualmente — apareceram como novos porque os manuais antigos estavam sem `contaId` ou em outra conta. v0.23.2 substituiu por **detecção em 3 níveis de confiança**, anotando `lanc.potencialMatch` em vez de excluir silenciosamente:

| Nível | Condições | Default UI | Caso típico |
|---|---|---|---|
| **ALTA** | mesma conta, ±R$ 0,01, ±2 dias | banner vermelho, default ignorar | importação repetida do mesmo extrato |
| **MÉDIA** | qualquer conta, ±R$ 0,01, ±7 dias | banner amarelo, default ignorar | gasto manual sem `contaId` ou em conta diferente |
| **BAIXA** | ±1% valor, ±15 dias, palavra-chave ≥4 chars compartilhada | banner cinza, default incluir | valor com juros/tarifa anexa, data de vencimento vs débito |

A UI mostra o gasto suspeito inline (nome, data, valor, categoria) pra Tiago decidir caso a caso. Sem mais "sumiu silenciosamente" nem "duplicou silenciosamente".
**Comercial:** Princípio: **probabilidade não é binária; UI deve refletir isso quando o custo do falso positivo e falso negativo são diferentes**. Dedup binário tem dois modos de falha graves: (a) falso positivo = silencia gasto legítimo (perdido pra sempre se usuário não revisou); (b) falso negativo = cria duplicata (tem que limpar depois). Níveis de confiança transformam o erro em **decisão informada do usuário**, com default sensato por nível. Aplicável em B2B em qualquer operação probabilística — reconciliação bancária, match de pedido com nota fiscal, dedup de cliente em CRM, detecção de fraude. Princípio derivado: cada nível tem uma combinação diferente de tolerâncias por feature (valor, data, descrição); nunca jogar tudo num único score (que esconde o porquê do match). Telemetria comercial: "quantos casos por nível por operação" → calibragem contínua dos thresholds.

### 60. Modo rascunho persistido em localStorage resolve modal que bloqueia o resto do app
**Hoje:** Tela de revisão de extrato é uma sheet modal que bloqueia o app. Tiago precisava conferir gastos antigos no histórico (Eleva, Wise) pra decidir se importar as duplicatas — mas fechar a sheet perdia todo o progresso de classificação. v0.23.2 introduziu **rascunho persistido**: `revisaoExtratoState` serializado em `localStorage` sob chave `financa.revisaoExtratoDraft` + timestamp. Botão "salvar e voltar depois" no rodapé fecha a sheet preservando estado. Quando Tiago volta ao botão "Importar extrato", `abrirSeletorExtrato()` detecta rascunho e abre modal `askChoice` com 3 opções (retomar / descartar / cancelar) antes do file picker. Auto-save a cada classificação inline. Limpeza automática após apply bem-sucedido.
**Comercial:** Princípio: **operações longas (>1 minuto) em modal precisam ter "interruptor seguro"**. Modais que bloqueiam navegação são apropriados quando a operação dura segundos; pra fluxos analíticos (revisão de batch, configuração complexa, formulários longos), o modal vira prisão. Padrão genérico aplicável: qualquer workflow multi-step com decisões deve auto-salvar progresso em storage persistente (localStorage no web, AsyncStorage no mobile, Postgres em B2B). Em B2B, rascunhos viram entidade fiscal — "imports não finalizadas" aparecem no dashboard do contador, com expiração configurável (limpar rascunhos >30 dias). Sub-pattern: ao retomar, oferecer SEMPRE a opção de descartar — usuário pode ter mudado de ideia, e forçar retomada de rascunho confuso é pior que recomeçar. Outro sub-pattern: rascunho indica progresso ao usuário ("você tinha 87 lançamentos, 32 classificados") — vira métrica de engagement e de qualidade do parser.

### 61. Classificação inline durante revisão > apply-then-edit
**Hoje:** v0.23.0/v0.23.1 só permitiam **incluir/ignorar** por linha. Categoria/subcategoria ficavam como default ("Tiago > a classificar") até depois do apply. Resultado: 50 gastos viravam 50 edições manuais no app depois — friction máxima que mata adoção. v0.23.2 adicionou **picker inline de classificação** por linha: botão tracejado "`Tiago > a classificar  ✎ editar`" abre modal sheet com chips de categoria (5 fixas com cor do membro) + select de subcategoria (auto-filtrada pela categoria escolhida) + select opcional de grupo. Salvar marca `l.classifManual = true` + dados; badge "✓ classificado" aparece. Filtro novo "❓ a classificar (N)" foca só no que falta. **Bonus retroativo:** quando Tiago aplica o lote, cada gasto com `classifManual` chama `aprenderAlias(descricao, categoria, subcategoria, grupoId)` — o sistema fica progressivamente mais inteligente sem esforço extra.
**Comercial:** Princípio: **friction em operação de classificação cresce linearmente com N; deve ser combatida no fluxo, não no fluxo seguinte**. Apply-then-edit é o anti-pattern clássico de importadores: "importa primeiro, classifica depois" parece simples no UX mas multiplica trabalho do usuário. Pattern correto: oferecer classificação inline OPCIONAL na própria revisão, default sensato pra quem pula, alias aprendido pra ganhar com o uso. Em B2B aplicável a: dedup de leads em CRM (campo de origem inline durante revisão), match de fatura com pedido (campo de centro de custo inline), reconciliação bancária (categoria contábil inline). Sub-pattern adicional: classificação inline + aprendizado de alias = **machine learning artesanal sem ML stack** — sistema fica melhor com uso, sem treinar modelo. Aliases também são **explicáveis e editáveis pelo usuário** (vs ML opaco), o que matters em B2B onde compliance exige auditoria de "por que isso foi classificado assim".

### 62. Aliases aprendidos retroativamente fecham o loop sistema-aprende-com-uso
**Hoje:** O parser de fatura usava `state.aliasItems` desde a Fatia 2.1 — quando Tiago classificava manualmente um item importado, o app guardava a associação "Netflix → Lazer/Entretenimento" e na próxima importação aplicava automaticamente. v0.23.1 estendeu esse comportamento pro parser de extrato (`buscarAliasParaDescricao` no pipeline de classificação automática). v0.23.2 fechou o loop em duas direções: (1) **alias é aplicado** se já existe quando o lance é classificado; (2) **alias é aprendido** quando Tiago classifica manualmente um lance que não tinha alias. Resultado: segundo extrato Itaú importa com ~80% pré-classificado (Tiago faz alguns toques na primeira, ganha automação na segunda em diante).
**Comercial:** Princípio: **memória de classificação é o segredo de baixar atrito de operações repetitivas pra próximo de zero**. Apps genéricos pedem categoria toda vez ("Pesado!"). Apps inteligentes lembram do passado. **Mas o pattern só funciona se for em ambas as direções:** sem aplicar, o usuário sente que classificou em vão; sem aprender, o usuário sente que ensinar não serviu. Ciclo virtuoso completo: (1) usuário classifica → (2) sistema aprende alias → (3) próxima vez sistema aplica → (4) usuário confirma sem esforço → (5) alias é reforçado (incrementa `contagemUso`, atualiza `ultimoUsoEm`). Em B2B (CRM, ERP, classificação fiscal), alias aprendido é diferenciador comercial óbvio — economia de horas/semana por usuário. Sub-pattern: aliases devem ser **editáveis e auditáveis pelo usuário** (lista em Config), com contagem de uso visível pra entender quais são frequentes. Anti-pattern: aplicar alias silenciosamente sem badge "📌 alias" — usuário não confia se não vê. Pattern correto: badge visível, microcopy explicativa.

### 63. Lista cronológica como complemento a agregados — fato vs métrica
**Hoje:** Os Rel 1/2/3 mostram totais agregados (por categoria, por subcategoria, por grupo, por mês). Excelente pra perguntas analíticas ("Quanto gastei em educação?"). Mas Tiago apontou que faltava a **vista fato** — listar gastos individuais cronologicamente para auditoria ("Quais foram as 12 compras de educação que somam R$ 6.500?"). v0.24.0 (Fatia 5c) introduziu o **Rel 4 — Detalhe**: chips de escopo (Família/Casa/Marina/Heitor/Juliana/Tiago), lista cronológica decrescente agrupada por dia, cada linha com identificação completa (cat › subcat › grupo) + método (conta/cartão) + badges (fixo/invest/parcela/origem). Cartão de resumo no topo (total + count + top 5 subcats com %). PDF exportável com título "Relatório Detalhado · [Escopo]". Tocar na linha abre `openGastoForm` pra editar — fechando o loop entre análise e ajuste.
**Comercial:** Princípio: **dashboards (métrica) e listas (fato) servem perguntas DIFERENTES — não é redundância, é complementaridade**. Métrica responde "quanto/quando/onde", fato responde "o que exatamente". Em B2B (auditoria fiscal, due diligence, compliance, conciliação), o fato é o produto entregável final — métricas são pra navegação até o fato. Aplicável a: livro contábil (lista cronológica de lançamentos por conta, por centro de custo); folha analítica (lista por funcionário com horas/proventos/descontos por dia); inventário (lista de movimentações por item). Sub-pattern: cada linha-fato deve ser **clicável pra abrir o detalhe completo** (mesmo formulário que cria o objeto). Cria continuidade entre "olhar relatório" e "agir sobre o que vi" — não força usuário a abrir outra tela. Princípio derivado: **toda visão analítica deve oferecer um drill-down até a transação individual original**; sem isso, o relatório vira "número sem rastreabilidade", o oposto do que auditoria exige. Em B2C diferencia produto profissional de joguinho de finanças.

### 64. Render parametrizado como fonte única entre tela e PDF
**Hoje:** A Fatia 5c.1 precisava da mesma lista cronológica em dois lugares: na tela do Rel 4 e no apêndice dos PDFs do Rel 1/Rel 3. Em vez de duplicar o loop de montagem, extraí `_relListaCronologicaHtml(gastos)` — recebe a lista JÁ filtrada e só renderiza (dia-separadores + linhas via `_rel4RenderRow`). Tela e PDF chamam a mesma função. Zero duplicação.
**Comercial:** Sempre que a "mesma informação" aparece em dois contextos (tela vs export, web vs e-mail, app vs API), deve sair de UMA função de render parametrizada — não de dois caminhos paralelos que divergem com o tempo. Caminhos paralelos são a origem clássica de "no app aparece X, no PDF aparece Y". Custo de extrair é baixo; custo de manter dois renders sincronizados manualmente é composto e silencioso.

### 65. Reconciliação por construção: o detalhe usa o mesmo conjunto+filtro do agregado
**Hoje:** O apêndice de detalhe do PDF (Rel 1/Rel 3) foi montado lendo EXATAMENTE as mesmas categorias (`relCategoriasIncluidas()` / `REL3_CATS_TIAGO`) e o mesmo filtro (`_relGastoSaida`) que o hero do relatório. O total do apêndice é igual ao do hero não por coincidência verificada em teste, mas **por construção** — ambos derivam da mesma definição. Simulação Node confirmou o invariante com Tiago OFF (R$ 2.000), Tiago ON (R$ 4.550, inclui invest+previsto) e período 3m (R$ 3.250).
**Comercial:** Continuação direta do item 54 ("uma pergunta = uma resposta"). A forma mais robusta de garantir consistência entre agregado e detalhe não é testar que dois cálculos batem — é fazer os dois lerem a MESMA fonte de definição. "Reconciliação por construção" > "reconciliação por teste". Em ERP/auditoria, agregado e detalhe que não reconciliam é falha de compliance; derivá-los do mesmo predicado elimina a classe inteira de bug.

### 66. Apêndice respeita o escopo temporal do relatório, não o hero
**Hoje:** O hero mostra só o mês corrente (granularidade constante — item da Fatia 5.1), mas o relatório multi-mês mostra N meses nos gráficos. O apêndice cronológico segue o PERÍODO selecionado (N meses via `relYmsHistorico`), não o hero. Assim o PDF é internamente coerente: agregados de N meses + detalhe dos mesmos N meses. Quando período = mês (default), apêndice e hero coincidem. O cabeçalho do apêndice declara o período em prosa pra remover ambiguidade.
**Comercial:** Documento de auditoria deve ter UM escopo temporal, explícito. Misturar "gráfico de 6 meses + lista de 1 mês" no mesmo arquivo gera leitura enganosa — o leitor assume que a lista é o detalhe do gráfico. Regra: todo bloco do documento declara seu próprio recorte temporal, e blocos do mesmo documento compartilham o recorte salvo indicação contrária visível.

### 67. Filtro multidimensional como interseção; a base sem filtro alimenta o catálogo de opções
**Hoje:** Os filtros de subcategoria e grupo no Rel 4 combinam por interseção (gasto passa se está numa subcat selecionada E num grupo selecionado). As opções de chip vêm da BASE (escopo+mês, sem filtros) via `_rel4Catalogo`, separada da lista efetivamente filtrada via `_rel4AplicarFiltros`. Separar "universo" (o que existe) de "recorte" (o que está marcado).
**Comercial:** Padrão de toda UI de filtro analítico. Computar as opções a partir do universo não-filtrado evita o bug clássico do "filtro que esconde suas próprias opções" (marco uma subcat, a lista de subcats encolhe pra só ela, não consigo marcar outra). Em BI esse comportamento é esperado e sua ausência é percebida como bug. Custo de implementar corretamente: manter base e filtrado como duas variáveis distintas — trivial se previsto desde o início.

### 68. Token CSS órfão (`--chip-color`) — auditar variáveis definidas mas nunca consumidas
**Hoje:** A variável `--chip-color` era definida inline em cada chip de escopo do Rel 4 desde a v0.24.0, mas NENHUMA regra CSS a consumia — o chip de escopo ativo não ganhava destaque de cor. Só apareceu porque fui ler o CSS antes de estendê-lo na 5c.1. A fatia adicionou `.rel-det-chip.active { background: var(--chip-color, var(--ink)); ... }`, que finalmente dá uso ao token e conserta o destaque de graça.
**Comercial:** Variáveis/tokens definidos mas sem consumidor são débito silencioso — parecem intencionais mas não fazem nada, e quem mexe no código depois assume que funcionam. Em escala, um lint de CSS deveria sinalizar "custom property definida com zero consumidores". Lição de processo: **ler o CSS-alvo antes de estender revela dead code que de outra forma persistiria indefinidamente** — a leitura prévia tem retorno além de evitar regressão.

### 69. Fatia fina protege o componente mais delicado (decisão de escopo: Rel 2 isolado)
**Hoje:** O plano combinado era "append cronológico em Rel 1/2/3". Ao implementar, o Rel 2 (regime de caixa) revelou estrutura genuinamente diferente: inclui ENTRADAS (que no schema não têm categoria nem membro), usa filtro próprio (`_rel2FluxoConta`) e cache de faturas pagas (`_rel2FaturaPaga`). O renderer de linha de gasto (`_rel4RenderRow`) não serve pra entradas. Decisão: entregar Rel 1 + Rel 3 (mesma estrutura, reuso limpo do render cronológico) na v0.25.0, e isolar o append do Rel 2 numa **Fatia 5c.2** dedicada, com renderer próprio de "extrato de fluxo" (entradas verde / saídas terracota / invest navy).
**Comercial:** Quando um item de um lote compartilhado tem estrutura genuinamente diferente dos demais, forçá-lo no mesmo código aumenta risco justamente no componente mais complexo. Isolar em fatia própria mantém o reuso onde ele é natural (itens similares) e protege o delicado. Princípio de fatiamento: **agrupar trabalho por similaridade de estrutura, não por simetria de roadmap**. "3 relatórios, então faço os 3 juntos" é simetria de roadmap; "2 têm a mesma estrutura e 1 é diferente, então 2+1" é similaridade de estrutura — a segunda é a correta.

### 70. Templates de skill usam nomes genéricos — mapear ao schema real antes de usar
**Hoje:** O reference `report-sections.md` da skill `financa-relatorio-pdf` traz templates de seção com nomes de campo genéricos (uma "categoria" temática por gasto). O schema real do Finança é assimétrico: `g.categoria` é o MEMBRO (`casa`/`marina`/`heitor`/`juliana`/`tiago`) e `g.subcategoria` é a categoria temática (Alimentação, Saúde…). Usar o template ao pé da letra teria gerado um relatório que não bate com o app. A Fatia 5d montou cada seção lendo o schema real (soma por membro via `categoria`, soma temática via `subcategoria`).
**Comercial:** Template de skill é ponto de partida, não contrato. A lição de processo: **ler o schema real antes de instanciar qualquer template** — o template descreve a FORMA da seção, não os nomes dos campos. Em qualquer ferramenta que gere artefato a partir de dados de terceiros, o passo "casar template com schema real" não é opcional.

### 71. Quando SKILL.md e seu reference file divergem, a SKILL.md é lei
**Hoje:** O reference `report-sections.md` incluía blocos de "taxa de poupança / runway / % comprometido". A `SKILL.md` (seção de anti-padrões) proíbe esses indicadores explicitamente. Os dois documentos da MESMA skill se contradiziam. Decisão na Fatia 5d: a `SKILL.md` prevalece — os indicadores ficaram de fora; o relatório reconcilia por construção (total = consumo + investimento = soma por membro = soma por subcategoria = soma dos meses).
**Comercial:** Hierarquia de fontes precisa ser explícita quando há mais de um documento normativo. Princípio adotado: **SKILL.md é a constituição; references são apêndices ilustrativos** — em conflito, a constituição vence. Sem essa regra, cada conflito vira decisão ad-hoc e o comportamento fica imprevisível.

### 72. Componente "de relatório" pertence ao app, não ao relatório — reuso em superfície de uso diário
**Hoje:** `_relListaCronologicaHtml` + `_rel4RenderRow` nasceram dentro do Relatório Detalhado (Rel 4). A Fatia 5e os reusou TAL QUAL numa sheet de "extrato do mês" acessível direto das telas Família e Tiago (uso diário), não da aba Relatórios (uso analítico). O único código novo foi a casca (sheet + busca + month-nav); a renderização de cada linha veio de graça, com badges e drill-down idênticos.
**Comercial:** Quando um componente é bem isolado (recebe lista, devolve HTML, sem estado escondido), ele transcende a tela onde nasceu. Lição: **nomear/isolar componentes pela responsabilidade ("renderiza lista cronológica"), não pelo lugar ("parte do Rel 4")** — o segundo amarra o reuso. Pagou dividendo: a feature mais pedida pelo Tiago (conferir extrato) saiu em horas, não dias.

### 73. Criar entidade inline > abrir 2º modal — empilhamento de z-index é frágil
**Hoje:** No picker de classificação do extrato (Fatia 5e), o botão "+ nova subcategoria" ia abrir o helper `askText` (modal Promise-based, z-index 200). Mas o overlay do próprio picker é z-index 250 — o `askText` apareceria ATRÁS, invisível. Em vez de mexer no z-index do helper compartilhado (risco de quebrar outros fluxos), a subcategoria nova passou a ser criada INLINE dentro do próprio picker (modo `criandoSub`: troca o `<select>` por input + "criar e usar"/"cancelar").
**Comercial:** Modais empilhados são uma fonte recorrente de bug de camada (z-index, foco, scroll travado, backdrop duplo) e de UX confusa em mobile. Princípio: **resolver a sub-tarefa no lugar (inline) em vez de empilhar contexto**. Além de evitar a fragilidade, a experiência fica melhor — o usuário não perde de vista o que estava classificando.

### 74. Capacidade latente no schema — campo que existe mas nunca é alimentado
**Hoje:** A função `aprenderAlias(...)` já tinha um parâmetro `identificacao` (nome amigável) desde a v0.11.0, e o objeto alias já guardava o campo — mas o fluxo de importação de EXTRATO sempre passava `null` ali e usava a descrição crua do banco como `nome` do gasto. A capacidade existia meio-implementada, esperando ser ligada. A Fatia 5e fechou o circuito: o nome que o Tiago digita na classificação vira o `nome` do gasto E é ensinado ao alias via `identificacao`; o ramo de auto-classificação por alias passou a aplicar `alias.identificacao` como nome. Próxima importação do mesmo lançamento já vem com o apelido bonito.
**Comercial:** Análogo ao token CSS órfão (item 68), mas pior: não é dead code, é uma feature 80%-pronta dormindo. Auditar não só "o que está definido mas sem consumidor" como também **"o que está cabeado pela metade — campo no schema, parâmetro na função, mas sempre passado vazio"**. Geralmente é a feature mais barata de completar (a infra já existe) e de alto valor percebido.

### 75. Conciliação de saldo como audit de ouro do extrato de conta
**Hoje:** Diferente da fatura (que tem "total da fatura" pra conferir — item 26), o extrato de conta não tem um total único. O equivalente é a **conciliação de saldo**: `saldo_anterior + Σcréditos − Σdébitos = saldo_final`. No parser de extrato Sicoob (Fatia 5b.3), isso fechou em **R$ 0,00** nos 32 lançamentos de maio. Telemetria em `window.__extratoDebug.auditSaldo`.
**Comercial:** Pra extrato de conta, a conciliação de saldo é o gate de confiança equivalente ao total bruto da fatura. Fechar em R$ 0,00 **prova matematicamente** que o parser não perdeu nem inventou nenhum lançamento. Em escala vira selo visível ("importação conferida ✓ · saldo reconciliado") — confiança auditável que apps genéricos (que importam best-effort, sem fechar conta) não oferecem. Pra um produto que se vende como "digno de auditor", a conta que fecha é o argumento.

### 76. Classificação por campo exclusivo do banco — extensibilidade sem regressão
**Hoje:** O `classificarLancamentosExtrato` nasceu pro Itaú. Pro Sicoob (5b.3), em vez de reescrever, adicionei um bloco guardado por `if (l.historico)` — campo que SÓ o parser Sicoob preenche. O Itaú não tem `historico`, então passa direto pra lógica antiga, intacta. Cada banco classifica do seu jeito sem encostar nos outros.
**Comercial:** Pattern de extensibilidade multi-banco. Adicionar banco novo = parser + bloco de classificação guardado por um campo exclusivo dele, com risco zero de regressão nos existentes. Em escala (dezenas de emissores), isolar por guarda evita o "mexe num, quebra outro" que trava roadmap de integrações. É o que permite crescer a cobertura de bancos sem virar campo minado.

### 77. Validar parser isolado em sandbox antes de transplantar pro arquivo grande
**Hoje:** O `index.html` tem ~18k linhas. Em vez de escrever o parser direto lá e testar no navegador, escrevi `parserExtratoSicoob` num arquivo Node isolado, rodei contra o **texto real extraído do PDF**, confirmei 32 lançamentos + conciliação R$ 0,00, e SÓ ENTÃO transplantei — depois validando o transplante (extraindo a função de volta do index e rodando de novo contra o mesmo texto).
**Comercial:** Método de engenharia pra base monolítica (ver item 1). Testar a lógica pura isolada (entrada→saída, sem o peso do app) torna o ciclo de teste segundos, não minutos, e **separa "a lógica está certa?" de "a integração está certa?"** — dois bugs diferentes, diagnosticados separados. É o paliativo que torna seguro evoluir parser num monolito enquanto a componentização (item 1) não chega.

### 78. Dashboard (vivo) × Relatório (estático) — duas necessidades distintas
**Hoje:** A tela de relatórios do app é interativa (navega meses, filtra, drill-down) — um **dashboard** pra monitorar e decidir agora. O **Relatório Elegante** é documento estático/arquivável pra comunicar e guardar. São coisas diferentes; por isso, na unificação, a tela NÃO virou A4 (decidido com o Tiago em 21/maio).
**Comercial:** Distinção de produto **fundamental pra versão de empresa**. Um cliente B2B vai querer um dashboard AO VIVO (faturamento do dia, contas a pagar da semana, fluxo projetado, atualizando em tempo real) E relatórios formais ESTÁTICOS (DRE do trimestre, relatório pro contador). Confundir os dois entrega mal os dois — documento que tenta ser interativo, ou tela viva que tenta ser arquivável. Arquitetura-alvo: separar a **camada de dashboard** (componentes reativos) da **camada de relatório** (renderizador de documento), ambas lendo a MESMA fonte de cálculo (a reconciliação por construção, item 65, garante coerência entre as duas).

### 79. Unificação progressiva — crescer o bom até aposentar o ruim
**Hoje:** Havia dois caminhos de PDF (o "rápido" cru via `window.print()` e o "elegante"). Em vez de manter os dois pra sempre ou refazer tudo, a decisão (21/maio) foi fazer o elegante **crescer** (ganhou fluxo detalhado, gastos por categoria, cronológico — Fatias 5c.2 a 5c.5) até cobrir o que o rápido faz, pra então aposentar o rápido. Migração sem big-bang.
**Comercial:** Padrão de evolução de produto sem reescrita arriscada. Entre "deletar o velho e construir o novo" (gap de função + risco) e "manter os dois pra sempre" (dívida + confusão), a terceira via é o artefato bom **absorver incrementalmente** as capacidades do ruim e só então aposentá-lo. Aplicável a migração de telas, features e até de stack — cada passo entrega valor e nada quebra no meio.

### 80. Controle agrupado que rege tela e saída juntos — "o que vê é o que sai"
**Hoje:** Os controles do Rel 1 (incluir Tiago / gastos por categoria / lista detalhada) estavam espalhados e com efeitos em PDFs diferentes. A Fatia 5c.4 agrupou os três numa **caixa única**, e cada interruptor passou a reger o que aparece TANTO na tela QUANTO no documento exportado — modelo mental único.
**Comercial:** Divergência entre preview e output (a tela mostra X, o PDF sai Y) é fonte clássica de frustração e ticket de suporte. Juntar controles relacionados + garantir "o que você vê é o que você exporta" reduz carga cognitiva e suporte. Em produto, paridade preview↔output é um princípio de confiança, não um detalhe.

### 81. Paridade entre superfícies que fazem a mesma tarefa
**Hoje:** Classificar um lançamento tinha duas portas — o picker da FATURA e o do EXTRATO. O da fatura já tinha criar-grupo e marcar-fixo; o do extrato não. O Tiago tropeçou nisso em uso real (Fatia 5e.1 deu a paridade: grupo inline + marcar fixo no extrato). Bônus de processo: a documentação do projeto apontava o gap no lugar ERRADO (dizia que faltava na fatura) — só auditar o código revelou que era no extrato.
**Comercial:** Quando a mesma tarefa tem mais de uma porta de entrada, elas precisam de **paridade de capacidades** — senão o usuário aprende numa e se frustra na outra. Em escala vira item de checklist de QA ("toda superfície de classificação tem o mesmo conjunto de ações"). Lição de processo embutida e reutilizável: **o código é a fonte de verdade quando a documentação e o comportamento divergem** — auditar o código antes de confiar na nota.

---

## Diferenciação real do produto

Manter destacado o que pode virar argumento comercial:

- **Família com membro isolado** — útil pra mães solo, casais com finanças parcialmente separadas, casas com pais idosos.
- **Eixos cross-cutting (grupo + subcategoria + identificação)** — único entre apps BR. Ex: rastrear todo gasto com "Honda Civic" cruzando IPVA + gasolina + seguro + manutenção, mesmo estando em subcategorias diferentes. Identificação adiciona granularidade 1-pra-1 pra streamings e assinaturas.
- **Parser nativo de fatura BR** (Fatia 3) — Itaú Black, Itaú Infinite, Sicoob, BTG são cartões premium do segmento high-income. Qualidade de extração vence apps genéricos.
- **Parser Itaú Visa Infinite robusto** (Fatia 3.1) — diferencial técnico não-trivial de replicar: layout 2-col com histograma de Xs, normalização de fragmentação de PDF.js, detecção adaptativa de sentido Y, corte ancorado da seção "Compras parceladas - próximas faturas" com seleção do melhor candidato por posição. 5 iterações de fix em validação real. Reflete investimento de engenharia que apps genéricos não fazem.
- **Parser Sicoob robusto com descrição multi-linha** (Fatia 3.2) — captura corretamente lançamentos que quebram em 2-3 linhas físicas (descrições longas com parcela X/Y no meio, ex: "IMUNOCENTRO SERV MED 01/05"). Usa âncora-de-DATA + agrupamento por Y adaptativo. Parser genérico perderia esses lançamentos silenciosamente — apps comerciais provavelmente perdem. Auditoria de total bruto pegou e validou (R$ 1.947,92 bate centavo a centavo com a meta).
- **Parser BTG Pactual** (Fatia 3.3) — terceiro banco com parser dedicado. Layout 3-col em case-mista ("Abr" não "ABR") com ordem invertida no texto agrupado por Y. Confirmou que o pattern dual-mode (X/Y + texto bruto fallback) é replicável: parser BTG implementado em ~150 linhas, validado em 3 cenários (items OK, vazios, null) — todos R$ 40,90 centavo a centavo. Próximo banco já cai num framework conhecido.
- **Compromissos do Mês** (Fatia 4) — visão consolidada de gastos fixos + parcelas projetadas, com 3 perspectivas (Família · Tiago · Total) e toggle pra incluir parcelas. Diferencial técnico: ao importar uma compra parcelada, o app materializa as parcelas faltantes nos meses seguintes (`isParcelaProjetada=true, pago=false`); quando a próxima fatura chega, ele **reconcilia** automaticamente em vez de duplicar. Usuário vê 12 meses de fixos projetados + parcelas projetadas até o fim do parcelamento. Em B2C, vira "vou ter R$ X comprometido em JUN" — visibilidade que apps genéricos não oferecem.
- **Relatório 1 — Gastos & Saídas em 4 lentes simultâneas** (Fatia 5.1, v0.16.0) — único entre apps BR a oferecer 4 modos de agrupamento (subcategoria, grupo, nome, identificação) num **mesmo relatório com drill-down universal**. Cada lente é uma seção empilhada (não tab) — apps brasileiros tipicamente forçam escolha por dropdown ou tabs. Pago + previsto separados visualmente (faixa tracejada pra previsto). Modo multi-mês com pilha por categoria + 2 curvas finas sobrepostas (mediana tracejada + média móvel suavizada via Bezier) + small multiples por categoria com tendência individual (↑ % / ↓ % / → estável). Grupos cross-cutting marcados com ✦ quando aparecem em mais de uma categoria. Toggle inline pra incluir Tiago como mais uma categoria — preserva o princípio "Tiago é isolado" sem perder a opção de visualizar o consolidado total.
- **Relatório 2 — Fluxo de Caixa em visão FINANCEIRA** (Fatia 5.2, v0.17.0) — único entre apps BR a separar com clareza **regime de competência** (Rel 1: cada gasto conta no mês da compra) de **regime de caixa** (Rel 2: cartão só conta quando a fatura é paga). Apps brasileiros típicos confundem os dois ou só oferecem o regime de competência. Hero de 3 colunas (Entradas verde · Saídas terracota · Investimentos navy) com mini-trend ("↑ X% vs mediana 6m") e saldo do mês (verde se positivo, terracota se negativo). Modo multi-mês com barras agrupadas (3 lado a lado por mês) + linha de saldo tracejada sobreposta. 4 lentes (entradas por fonte · saídas por categoria · investimentos por subcat · saídas por conta de origem) com drill-down universal — drill-down de conta mostra "qual conta sangrou mais este período" útil em multi-conta brasileiro (Itaú + Sicoob + BTG). Pagamento de fatura aparece naturalmente como saída no mês do débito automático — visualização honesta do fluxo real do caixa do mês.
- **Relatório 3 — Meus Gastos com comparativo opcional** (Fatia 5.3, v0.19.0) — pattern de "vista isolada com comparativo opt-in via toggle". Tiago tem sua vista própria (cor verde, gastos próprios), mas pode ativar "Comparar com a família" pra ver lado a lado Tiago vs Família com percentuais, sem perder o foco do Tiago. Diferencial cognitivo: o toggle muda **enquadramento** sobre os mesmos dados, não controla visibilidade — solo vs comparativo são duas perspectivas válidas. Small multiples adaptados: em vez de mini-gráficos por membro (como no Rel 1), são mini-gráficos por subcategoria do Tiago (top 4) com mediana + tendência individual. Aplicação B2B clara: relatórios de centro de custo onde uma área específica quer se comparar com o resto da empresa sem perder o foco da própria operação.

- **Regimes contábeis explícitos e visualmente coerentes** (sessão 17/maio, v0.20.x) — o Finança trata regimes contábeis como cidadãos de primeira classe: Rel 1 é regime econômico (gasto conta no mês em que aconteceu, mesmo que pago depois) e Rel 2 é regime de caixa (só conta o que saiu da conta no mês). A diferença é técnica mas frequentemente confunde usuários — apps brasileiros típicos misturam os dois ou só oferecem o regime econômico. Solução implementada: microcopy em itálico discreto em cada hero ("regime econômico: gastos contam no mês em que aconteceram" / "regime de caixa: compras de cartão entram no mês em que a fatura é paga"). Recorrente no contexto onde aparece, não num FAQ separado. Pra finanças pessoais, isso é UX educativa contínua. Pra B2B (contabilidade/BI), é o tipo de detalhe que separa "ferramenta confusa que precisa de treinamento" de "ferramenta que ensina enquanto é usada".

- **Compromissos do mês como visão estratégica complementar** (Fatia 5.3-extras, v0.21.x) — pra além do "quanto gastei", o Rel 1 do Finança responde "quanto do mês já está pré-comprometido com recorrências fixas e parcelas de cartão em curso". Visualização: 1 coluna stacked grande (parcelas em mostarda escura em cima, fixos em terracota embaixo) com legenda lateral discriminando os valores. Cores **bem distintas** comunicam que são naturezas diferentes (fixos = recorrência indefinida, parcelas = compromisso finito até quitar). Drill-down em cada item. Diferencial: a maioria dos apps brasileiros não separa "parcelas em curso" de "gastos do mês" — confunde o usuário sobre qual é o "mínimo inegociável" do mês. Aplicação B2B: "compromissos firmes" no fluxo de caixa empresarial — folha + aluguel + financiamentos vs operação variável.

- **Auditoria financeira consistente** (v0.22.2) — toda métrica do app tem uma definição canônica e dá a MESMA resposta independente de onde for consultada. Sistemas concorrentes frequentemente mostram "Total comprometido" diferente em telas diferentes (cada uma com sua lógica) — drift silencioso que destrói confiança. Diferencial: princípio explícito + reconciliação cruzada verificada com números reais. Aplicação B2B (ERP, contabilidade, fintech regulada): requisito de compliance, não nice-to-have.
- **PDF profissional com texto selecionável** (Fatia 5.5, v0.22.0) — exportação via `window.print()` + CSS `@media print` em vez de gerar imagem rasterizada como apps casuais. Auditor pode buscar valores (`Cmd+F`), copiar números pra planilha, fazer OCR/extração, anexar em sistemas de gestão. Texto, não screenshot. Diferencial vs ferramentas de share rápido tipo Splitwise/Pluggy (que exportam imagem). Aplicação B2B: compatibilidade com fluxos de auditoria fiscal, anexo em processos jurídicos, integração com sistemas downstream.
- **Cor como informação, não decoração** (v0.21.2) — no gráfico "consumo · por subcategoria" do Rel 1, cada coluna ganha a cor do membro que mais contribui na subcategoria, revelando visualmente onde mora cada despesa (Moradia → Casa, Esporte → Heitor, etc.). Apps genéricos usam cor única ou paleta arbitrária. Diferencial: paleta carrega significado, não decora. Aplicação B2B (BI/analytics): dimensão real dos dados codificada em cor amplifica leitura; paleta arbitrária é ruído visual que dilui atenção.
- **Investimentos hierárquicos com fallback elegante** (Fatia 5.3-extras, v0.21.0) — pra evitar poluição cadastral (criar 1 subcategoria por tipo de investimento quando o usuário faz 1 aporte por mês em cada), o Finança permite cadastrar **uma modalidade só** ("Investimento") em Config e diferenciar tipos via identificação ou grupo (livres por lançamento). Os relatórios agrupam por: identificação se preenchida → senão grupo → senão subcategoria. Tag visual (`•` `🔗` `—`) indica origem do agrupamento. Auto-degradação elegante: funciona mesmo se ninguém preencher nada extra. Aplicação B2B: classificação flexível de transações (CRM/ERP/contabilidade) sem forçar taxonomia rígida — o usuário preenche o que faz sentido na hora, sistema agrupa pela informação mais específica disponível.
- **Importação de extrato com conta que fecha** (Fatia 5b.3, v0.29.0) — segundo banco no framework de **extrato de conta** (depois do Itaú; distinto dos parsers de fatura de cartão). Layout próprio do Sicoob: data sem ano (vem do período do header), valor com sufixo D/C/* colado, descrição quebrada em várias linhas. A **conciliação de saldo** (saldo anterior + créditos − débitos = saldo final) fechou em **R$ 0,00** nos 32 lançamentos de maio — prova de integridade de que nada foi perdido nem inventado. O pagamento da fatura do cartão é detectado e marcado pra não duplicar com as compras já lançadas. Apps genéricos importam extrato "best-effort" e nunca fecham a conta; o Finança concilia e mostra o resultado. Aplicação B2B: conciliação bancária com prova de integridade — requisito de quem leva contabilidade a sério, não nice-to-have.
- **Relatório elegante com profundidade sob demanda** (Fatias 5c.2–5c.5, v0.30.0→v0.33.0) — o mesmo documento apresentável carrega, cada camada ligável por interruptor: fluxo de caixa lançamento-a-lançamento (Rel 2), gastos detalhados por categoria/membro (total no topo → subcategoria com subtotal → itens) e lista cronológica completa (Rel 1/3). Tudo reconcilia com os totais da capa por construção. O usuário escolhe a densidade — resumo executivo pra apresentar ou documento completo pra auditar — sem recalcular nada e sem trocar de ferramenta. Aplicação B2B: o mesmo dado vira o one-pager pra diretoria E o anexo detalhado pro contador, controlado por um toggle.
- **Privacidade por padrão** — saldos escondidos, dados isolados, "tap to reveal" estilo private banking. PDF nunca sai do dispositivo.
- **Memória de classificação automática** (Fatia 2.1) — aprende com cada atribuição. Atrito mensal cai a quase zero depois de 2-3 importações.
- **Re-aprendizado em edições** (v0.9.6) — corrigir manualmente um gasto também atualiza o alias correspondente. Sistema fica mais inteligente conforme o uso.
- **Alias estendido com identificação e marca de fixo** (Fatia 3.1.5) — sistema não só aprende "Netflix é Lazer/Entretenimento", aprende também "Netflix é recorrente" e "Netflix é o apelido que usuário usa". Refinamento progressivo automático que apps genéricos não oferecem.
- **Totais consolidados unificados** (v0.9.7) — uma única visão que agrega gastos por grupo (quando há) ou por nome (quando não há). Resolve o caso "todos os iFoods juntos" naturalmente.
- **Extrato como gap-filler** (Etapa 5b, item 16) — importar sem medo de duplicata. Apps genéricos não fazem isso.
- **Auditoria de total bruto** (item 26) — em escala comercial, pode virar feature visível ("✓ R$ R$ X,XX confere com a fatura") + telemetria de qualidade de parser por banco.
- **Undo de importação em lote** (Etapa 5b, v0.23.1) — toda importação cria um lote rastreável; usuário desfaz uma importação inteira a qualquer momento, sem afetar gastos manuais. Migração retroativa idempotente cobre importações feitas antes do sistema existir. Apps de finanças genéricos não oferecem isso — quando o usuário erra na importação, vira faxina manual de horas. Aplicação B2B: rastreabilidade fiscal de operações em lote é hard requirement (ERP, contabilidade, auditoria).
- **Dedup probabilístico em 3 níveis** (Etapa 5b, v0.23.2) — em vez de excluir silenciosamente "possíveis duplicatas", o app apresenta o gasto suspeito inline com nível de confiança (alta/média/baixa) e default sensato por nível; usuário decide caso a caso. Resolve o caso real "manual antigo sem contaId" ou "registro em conta diferente" que dedup binário perderia.
- **Modo rascunho persistido** (Etapa 5b, v0.23.2) — operações longas como revisão de batch podem ser pausadas e retomadas. Localstorage guarda estado completo da revisão (filtros, classificações inline, decisões de incluir/ignorar). Modal não vira prisão — usuário sai do fluxo pra conferir histórico do app e volta sem perder progresso.
- **Classificação inline durante importação + alias retroativo** (Etapa 5b, v0.23.2) — picker de categoria/subcategoria/grupo por linha durante a revisão; quando aplica, classificações manuais viram aliases pra próxima importação. Sistema fica progressivamente mais inteligente sem treinar modelo de ML — "machine learning artesanal" auditável e editável pelo usuário.
- **Relatório Detalhado por escopo com lista cronológica** (Fatia 5c, v0.24.0) — quarta vista de relatório, complementar às vistas analíticas (Rel 1/2/3). Lista cada gasto individual cronologicamente, agrupado por dia, com identificação completa (cat › subcat › grupo + método + badges). Exporta PDF com título por escopo ("Relatório Detalhado · Heitor"). Toque na linha abre o gasto pra edição — fechando o loop análise→ação. Diferencia produto profissional (auditoria fiscal) de joguinho de finanças. Aplicação B2B: livro contábil, folha analítica, inventário — sempre que auditoria exige rastreabilidade até a transação individual.
- **Filtros finos dentro do escopo no Relatório Detalhado** (Fatia 5c.1, v0.25.0) — dentro de cada escopo (membro ou família), o usuário recorta a lista por subcategoria e/ou grupo, com chips multi-seleção que combinam por interseção. As opções de filtro vêm do universo não-filtrado (não somem conforme se seleciona). O recorte entra no título do PDF ("Heitor · Escola, Esporte"). Aplicação B2B: auditoria dirigida — "me mostre só os lançamentos de manutenção do veículo X no centro de custo Y" sem exportar a planilha inteira e filtrar à mão.
- **Agregado + detalhe reconciliados no mesmo PDF** (Fatia 5c.1, v0.25.0) — opção de anexar a lista cronológica como apêndice ao PDF dos relatórios analíticos (Rel 1 e Rel 3). O total do apêndice é construído da mesma definição do agregado, então reconcilia por construção (não por coincidência). É o formato que auditor profissional espera: o número-síntese e a lista que o compõe no mesmo documento, conferíveis lado a lado. Apps casuais entregam ou só o gráfico ou só a lista; o diferencial é a reconciliação verificável num único arquivo. Aplicação B2B: relatório fiscal/contábil onde "total + memória de cálculo" é requisito, não conveniência.

- **Relatório elegante para compartilhar** (Fatia 5d, v0.26.0) — rota de exportação separada do "PDF rápido": um documento HTML autônomo (Montserrat, capa, gráficos SVG, layout A4) pensado pra mostrar pra quem não usa o app — cônjuge, contador, sócio. Difere do PDF de auditoria (texto cru, denso) por ser legível por leigo. Os dois botões coexistem: "PDF rápido" (imprime a tela) e "✦ Relatório" (documento caprichado). Reconcilia com o app por construção (usa os mesmos motores de cálculo das telas). Aplicação B2B: o mesmo dado vira tanto o anexo fiscal denso quanto o one-pager apresentável pra diretoria — sem recalcular nada.
- **Extrato de conferência com busca** (Fatia 5e, v0.27.0) — botão "Ver extrato do mês" nas telas Família e Tiago abre uma lista cronológica achatada de TODOS os lançamentos (em Família, os 4 membros juntos), com busca livre por nome/categoria/valor. Nasceu da dor real de bater o extrato do banco contra o app sem precisar caçar lançamento por categoria. Os selos "❓ a classificar" / "📥 extrato" / "📄 fatura" deixam óbvio o que falta tratar. Aplicação B2B: conciliação bancária — o operador confere o extrato da conta contra os lançamentos do sistema numa tela só.
- **Classificação que aprende o apelido** (Fatia 5e, v0.27.0) — ao classificar um lançamento importado do extrato, o usuário troca a descrição crua do banco ("PIX 12345 FULANO") por um nome que reconhece ("Mercado") e pode criar subcategoria na hora. O sistema memoriza o apelido (via alias) — na próxima importação do mesmo lançamento, nome e subcategoria já vêm prontos. "Machine learning artesanal" agora também aprende o nome amigável, não só a categoria. Aplicação B2B: normalização progressiva de descritores bancários crus em rótulos contábeis legíveis, sem treinar modelo.

---

## Histórico de adições

- **11/maio/2026** — Documento criado durante Fatia 1.5. Tiago pediu pra registrar em paralelo sem abrir discussão imediata.
- **12/maio/2026** — Adicionados pontos 11 (aliases multi-camada como diferencial comercial) e 12 (parser PDF client vs server) após implementação da Fatia 2.1 (memória de classificação automática).
- **12/maio/2026 (mais tarde)** — Adicionados pontos 13 (data vencimento vs data compra), 14 (heurística de extração de chave) e 15 (dedup automática). Sessão fechando v0.9.7.
- **13/maio/2026** — v0.10.0 entregue (Fatia 3.0: dispatcher por banco). Discussão de arquitetura da Etapa 5b. Adicionado item 16 (extrato como gap-filler) e bullet correspondente em "Diferenciação real do produto". Item 15 reescrito pra deixar claro que cobre o caso fatura, enquanto o item 16 cobre o caso extrato — são casos de uso e algoritmos de dedup distintos.
- **14/maio/2026** — Fatia 3.1 completa (parser Itaú Visa Infinite, v0.10.5) + Fatia 3.1.5 nova (campo identificação + marca fixo, v0.11.1). Adicionados 5 itens técnicos: 17 (detecção de banco por marcadores múltiplos — logo é imagem), 18 (PDF.js fragmenta acentuados — normalização vira camada de infra), 19 (Y axis PDF.js nativo vs screen-native), 20 (identificação 1-pra-1 vs grupo 1-pra-N), 21 (isFixo em gasto importado sem fixedExpense). Item 11 atualizado pra refletir que aliases agora aprendem identificação e isFixo também. Adicionados 2 bullets em "Diferenciação real do produto": parser Itaú Visa Infinite robusto (diferencial técnico) e alias estendido com identificação/fixo (diferencial UX). Bullet de eixos cross-cutting atualizado pra incluir identificação.
- **15/maio/2026** — Fatia 3.2 completa (parser Sicoob, v0.12.0). Auditoria com PDF real bateu centavo a centavo: 11 lançamentos extraídos (10 consumo + 1 anuidade), soma R$ 1.947,92 idêntica à meta. Adicionados 5 itens técnicos: 22 (descrição multi-linha em layout colunar — pattern reutilizável via âncora-de-DATA), 23 (floating-point edge case com Y na fronteira da tolerância — tolerância sempre > gap medido), 24 (`parseValor` global do app não trata US com milhar — débito técnico a corrigir), 25 (anuidade do cartão como gasto vs encargo — decisão opinionada de produto), 26 (auditoria de total bruto como gate obrigatório do pipeline — em escala vira telemetria + feature visível). Adicionados 2 bullets em "Diferenciação real do produto": parser Sicoob robusto e auditoria de total bruto.
- **15/maio/2026 (mais tarde)** — v0.12.1 → v0.12.2 → v0.12.3 (3 patches no mesmo dia depurando o mesmo bug). Teste real em Safari iOS PWA mostrou que parser produzia 9 lançamentos / R$ 1.693,67 em vez de 11 / R$ 1.947,92. **As duas primeiras tentativas de diagnóstico estavam erradas**: v0.12.1 achou que `pdfItems` chegava vazio em Safari (implementou dual-mode A/B); v0.12.2 achou que modo A retornava parcial por quirk de Y (implementou best-of-N). **Bug real, descoberto na v0.12.3**: o `detectarBancoFatura` retornava `'itau-mc-black'` para a fatura Sicoob porque o produto se chama "Sicoob Mastercard Black Pro" — o detector checava `mastercard\s+black` ANTES de checar `sicoob`. Pista que teria poupado tempo: `debug: indisponível` significa "este parser não rodou" (porque `__faturaDebug` só é populado pelo parserSicoob). v0.12.3 inverteu a ordem do detector: emissor (Sicoob/BTG/Itaú) primeiro, produto/bandeira (Mastercard Black/Visa Infinite) depois. Testes de regressão (6/6 passaram). Apesar dos diagnósticos errados, o dual-mode A/B e o best-of-N permanecem como robustez genuína. Adicionados/refinados itens técnicos: 27, 27.1, 28, 29, 30.
- **15/maio/2026 (noite)** — Fatia 3.3 completa (parser BTG Pactual, v0.13.0). Auditoria com PDF real bateu centavo a centavo: 1 lançamento (Spotify R$ 40,90), pagamento de fatura corretamente filtrado por valor negativo, mensalidade isenta (R$ 0,00) também filtrada. Layout BTG é 3-col em case-mista ("Abr" não "ABR"), e o texto agrupado por Y sai com ordem invertida "VALOR DESC DATA" (em vez de "DATA DESC VALOR" do Sicoob). Modo A imune (usa coordenadas X/Y direto); modo B usa 2 regexes (invertido + visual) e tenta ambos. Validação em 3 cenários: items OK, items vazios, items null — todos R$ 40,90. Adicionados itens técnicos: 31 (ordem invertida no texto agrupado entre BTG e Sicoob — modo A é fonte da verdade, modo B precisa ser tolerante a ambas ordens), 32 (duplicação crescente de código entre parsers — pendente consolidação em parserFaturaColunar configurável). Adicionado bullet em "Diferenciação real do produto": parser BTG como terceiro banco no framework dual-mode. Aplicada com sucesso a lição da v0.12.3 (item 30): conferi detector ANTES de implementar parser — economizou tempo.
- **16/maio/2026** — Fatia 4 completa (Compromissos do Mês, v0.14.0). Visão consolidada de fixos + parcelas projetadas, com 3 perspectivas (Família/Tiago/Total) acessíveis via botão "📋 Compromissos" no topo das views. Toggle pra incluir parcelas no total. Navegação por meses. Diferencial técnico: pattern híbrido **materialização + reconciliação**. Ao importar compra parcelada (ex: IMUNOCENTRO 01/05 em MAI), o app cria 4 projeções nos meses seguintes (02-05/05) com `isParcelaProjetada=true, pago=false`. Quando próxima fatura chega com 02/05 real, o app reconcilia (atualiza valor/data/marca pago) em vez de duplicar. Fixos projetados pra 12 meses à frente, todos `pago=false` (usuário confirma quando paga). Migração one-time `migrarParcelasV014` no boot: gera `parcelaChave` em gastos existentes + materializa parcelas faltantes pra compras parceladas já no banco. Marcador `state.schemaVersion='0.14'` evita re-execução. Adicionados 3 itens técnicos: 33 (pattern híbrido materialização+reconciliação), 34 (chave de reconciliação estável via descricaoBruta, separando "o que sistema viu" de "o que usuário classificou"), 35 (schemaVersion como controle de migrations client-side). Adicionado bullet em "Diferenciação": Compromissos do Mês como visão de planejamento financeiro real. Validação: 6/6 testes lógicos com state sintético — materialização cria projeções, idempotência, reconciliação atualiza sem duplicar, fixos projetados 12m, migração one-time, migração idempotente.
- **16/maio/2026 (mais tarde)** — v0.14.1 (patch fix de coerência). Identificado problema crítico: ao desativar/remover/editar um fixedExpense (ex: cancelar Netflix), as projeções futuras já criadas continuavam no state. Fix: `limparProjecoesFuturasDoFixo(fixoId)` remove gastos `parentFixoId && pago=false && ym>corrente` (preserva pagos = histórico imutável); `atualizarProjecoesFuturasDoFixo(fixoId)` sincroniza projeções com edições do fixo. Hooks nos 3 pontos da UI: pausar limpa futuras (reativar recria via `processarFixosProjetados`), editar atualiza futuras, remover limpa futuras. Validação 5/5 cenários: pausar Netflix paga limpa 11 futuras e mantém o pago do mês; reativar recria 11; editar valor 30→45 atualiza só as futuras pendentes; remover limpa preservando histórico pago; edit não toca em gasto JÁ pago do mês corrente. Adicionado item técnico 36 (sincronização CRUD com projeções — regra de ouro: gastos pagos são FATOS imutáveis, gastos projetados são PLANOS sincronizáveis).
- **16/maio/2026 (noite)** — v0.14.2 (Fatia 4-extensão, fechamento do pattern simétrico de sincronização). Pendência declarada no item 36 da v0.14.1 atendida: quando usuário deleta um gasto-parcela manualmente, as projeções futuras com mesma `parcelaChave` (pago=false, ym>corrente) são removidas. Função nova: `limparProjecoesFuturasDeParcela(parcelaChave)`. UX: askConfirm pré-delete inclui aviso "⚠️ esta compra parcelada tem N parcelas projetadas em meses futuros — também serão removidas" se houver projeções; toast pós-delete mostra o N. Validação 4/4 cenários: deletar parcela paga 01/05 limpa 4 projeções e preserva compra não relacionada; deletar projeção do meio limpa as outras futuras com mesma chave; gasto sem parcelaChave é no-op; cenário misto preserva parcelas pagas no passado e limpa futuras pendentes. Item 36 das NOTAS atualizado (remove menção a "pendente fatia futura"). Próximo: Etapa 4 (pagamento de fatura via débito automático no banco do próprio cartão — Sicoob→conta Sicoob, Itaú→conta Itaú, BTG→conta BTG; informação operacional crítica passada pelo Tiago).
- **16/maio/2026 (madrugada)** — v0.15.0 (Etapa 4 — Pagamento de fatura). Fecha o ciclo cartão→conta: ao marcar fatura paga (manual via botão no drill-down do cartão OU automático no boot pra cartões com `debitoAutomatico=true` e vencimento já passado), o app cria uma **"saída técnica"** com `isPagamentoFatura: true`, `cardId: null` (pra não entrar na própria fatura), `contaId: card.contaPagamentoId` (debita a conta certa do banco emissor). Recalculo automático nos hooks de criar/editar/deletar gasto (e na importação de fatura): se a fatura já estiver paga, ajusta o valor da saída técnica; se ficar vazia, remove. Reversão via botão "desfazer" sempre disponível. UI: widget de status no drill-down do cartão (paga em DD/MM · R$ valor · conta) com botão de ação contextual; renderização especial no extrato da conta com tag "🔄 Pagamento fatura [nome] · MMM/AA". Schema novo: 3 campos opcionais (`isPagamentoFatura`, `faturaCardId`, `faturaYm`) sem migração disruptiva. Validação: 9/9 cenários — marcar paga cria saída na conta certa; idempotência via erro; recalc após edit; zerar fatura remove saída; desfazer; cartão sem conta bloqueia; automático só pra débito automático + vencido; idempotência do automático; fatura futura não paga. Adicionado item técnico 37 (pattern "saída técnica vinculada" como reuso do objeto-base do domínio com flags + FK reversa — princípio derivado: fontes de cálculo devem ser separáveis das saídas do cálculo, evita loops).
- **16/maio/2026 (manhã)** — Fatia 5.1 completa (Relatório 1 — Gastos & Saídas, v0.16.0). Primeira fatia da Etapa 5 dos Relatórios. Estrutura: header com nav-mês + seletor de período (mês/3m/6m/12m) + toggle "Incluir gastos de Tiago como mais uma categoria"; hero do mês com total + pago + previsto + % comprometido em fixos; 4 lentes empilhadas — (1) total consolidado: 1 coluna stacked por categoria com legenda completa, (2) por categoria: 4-5 colunas com subcategoria empilhada e gradiente derivado matematicamente, (3) modo multi-mês quando período > mês: N colunas stacked + 2 curvas finas sobrepostas (mediana tracejada `stroke-width=1 dasharray=2,3 opacity=0.55` + média móvel suavizada Bezier `stroke-width=1.25 opacity=0.78 tensão=0.18`), (4) small multiples 2x2 com mediana + tendência individual por categoria (↑/↓/→ baseado em delta MM[0]→MM[N-1] com threshold 5%); 3 lentes de agrupamento empilhadas — grupos cross-cutting (chips, ✦ quando aparece em >1 categoria), top nomes (agrupados por `normNome`, top 10), top identificações (agrupados por `identificacao.toLowerCase()`, top 10). Drill-down universal via `sheet.open()` em qualquer faixa de subcategoria, chip de grupo, ou linha de nome/identificação. Filtro mestre: `_relGastoConta` exclui `isInvest`, `isAjuste`, `isPagamentoFatura` (consumo puro). Decisão arquitetural: **SVG inline manual** pros 4 gráficos em vez de Chart.js (que está no head pra futuras fatias) — controle pixel-perfect das duas curvas sobrepostas + cores derivadas em runtime + zero JS de runtime extra. Adicionados 3 itens técnicos: 38 (SVG vs Chart.js — bibliotecas genéricas pra problemas genéricos; quando viz é específica, SVG direto é melhor), 39 (hero sempre mostra mês corrente, mesmo em multi-mês — âncora temporal estável), 40 (lentes empilhadas vs tabs — empilhadas convidam comparação, tabs escondem). Adicionado bullet em "Diferenciação": Relatório 1 com 4 lentes simultâneas + drill-down universal. ~1077 linhas adicionadas no `index.html` (11472 → 12549). Validação: `node --check` sintaxe OK, 0 NBSPs, 0 triple-backticks, versão única no rodapé. Próximo natural: **Fatia 5.2 — Relatório 2 (Entradas/Saídas/Investimentos)**.
- **16/maio/2026 (tarde)** — Fatia 5.2 completa (Relatório 2 — Fluxo de Caixa, v0.17.0). Segunda fatia da Etapa 5. Introduz a **visão FINANCEIRA** complementar à visão econômica do Rel 1 — cartão de crédito não conta na compra, só no pagamento da fatura. Filtro mestre `_rel2FluxoConta(g)` exclui `isAjuste`, `pago=false`, `pagamento='cartao'`; pagamentos de fatura entram naturalmente (têm `pagamento='conta'` e `pago=true`). Estrutura: pills no topo da view trocam entre Rel 1 e Rel 2 (refator de `renderRelatorios()` pra dispatcher; lógica da 5.1 renomeada pra `_renderRel1Inner()`); hero de 3 colunas (Entradas verde · Saídas terracota · Investimentos navy) com mini-trend vs mediana 6m e saldo do mês (verde se positivo, terracota se negativo); gráfico multi-mês com barras agrupadas (3 por mês: E·S·I) + linha de saldo sobreposta (Bezier suavizada tracejada fina); 4 lentes empilhadas (entradas por fonte de `state.fontesRenda`, saídas por categoria, investimentos por subcategoria, saídas por conta de origem) — todas com drill-down universal. Microcopy honesta sobre assimetria do schema: entradas não têm campo categoria, então o toggle Tiago só afeta saídas/invest; quando ON, "*entradas: família · saídas + invest: família + Tiago*". Estado independente por sub-vista (`relState` e `rel2State` preservados ao alternar tab). Adicionados 3 itens técnicos: 41 (visão econômica vs financeira como funções puras sobre mesmo state), 42 (dispatcher com pills resolve "vista com sub-vistas"), 43 (schema asymmetry → microcopy honesta vence simulação de simetria). Adicionado bullet em "Diferenciação": Relatório 2 separa regime de competência vs regime de caixa. Validação: `node --check` OK, 0 NBSPs, 0 triple-backticks, 13.339 linhas (+790 desde v0.16.0). Próximo: **Fatia 5.3 (Relatório 3 — Meus Gastos · Tiago)**.
- **16/maio/2026 (tarde, refinamentos)** — Ajustes v0.18.0 sobre as Fatias 5.1 e 5.2 entregues mais cedo na mesma sessão. Cinco mudanças de UX: (1) valor total das colunas do gráfico "por categoria" do Rel 1 em Fraunces 13px (era sans 10px); (2) colunas mais finas em geral no Rel 1 (`max-width: 52px`, gap 18px); (3) NOVA seção no Rel 1 "consumo · por subcategoria (top 10)" — top subcategorias consolidando todas as categorias incluídas, scroll horizontal, drill-down universal; (4) NOVA seção no Rel 2 "composição do mês" — 3 colunas grandes (Entradas stacked por fonte, Saídas stacked por categoria, Saldo divergente), drill-down em cada segmento; (5) Rel 2 multi-mês reformulado — linha tracejada de saldo substituída por 4ª coluna divergente por mês. Adicionados 3 itens técnicos: 44 (tudo colunas, nada linhas — princípio estético em mobile), 45 (subcategorias consolidadas como projeção 1D de dados 2D), 46 (escala honesta vs adaptativa). 13.673 linhas no `index.html` (+334 desde v0.17.0).
- **17/maio/2026** — Fatia 5.3 completa (Relatório 3 — Meus Gastos · Tiago, v0.19.0). Terceira fatia da Etapa 5. Pill "Meus Gastos" adicionada em REL_TABS; toggle "Comparar com a família" (off por padrão) muda enquadramento sobre os mesmos dados — quando OFF, hero solo + multi-mês com curvas (mediana + média móvel) do Tiago; quando ON, hero com 2 colunas (Tiago verde `#4A6741` + Família terracota `#A8462C` com %) + total comparativo (Tiago sólido vs Família stacked por membro) + multi-mês com barras agrupadas Tiago/Família. Sempre só Tiago: gráfico por subcategoria (top 10), small multiples por subcategoria (top 4 quando multi-mês), grupos cross-cutting, top nomes, top identificações. Reuso máximo da infra do Rel 1 — todas as funções `relCalc*` já aceitavam `cats` como parâmetro desde a Fatia 5.1, então cálculos foram chamados com `['tiago']` ou `['casa','marina','heitor','juliana']` sem código novo. Drill-downs separados `_rel3Drill*` filtram em `categoria === 'tiago'` direto. Schema sem mudanças. Adicionados 2 itens técnicos: 47 (parametrização preventiva paga dividendos compostos — decisão tomada na Fatia 5.1 rendeu 50% de redução de esforço na 5.3), 48 (toggle muda enquadramento, não disponibilidade — pattern de UI analítica). Adicionado bullet em "Diferenciação": Rel 3 com pattern "vista isolada com comparativo opt-in" aplicável a relatórios de centro de custo B2B. Validação: `node --check` OK, 0 NBSPs, 0 triple-backticks, 14.296 linhas (+611 desde v0.18.1). Próximo: **Fatia 5.4 (Relatório 4 — Completo, possivelmente trivial) ou direto pra 5.5 (Exportação PDF)**.
- **17/maio/2026 (sessão extendida tarde/noite)** — Sequência de 7 versões consolidando refinamentos conceituais profundos da Etapa 5. **v0.18.1 fix labels visuais** (overflow:hidden cortando valores e nomes). **v0.19.1 fix HANSARD invest** — sistemas paralelos `state.investMods` (UI) e `state.subcatsInvestimento` (filtro) unificados via `_isInvestRuntime(g)` aplicado em 17 lugares + migration silenciosa idempotente. **v0.20.0 invest no total** — decisão original do projeto restaurada (invest soma na saída, mas separa visualmente como navy); novo filtro `_relGastoSaida(g)` + nova seção "investimentos · por modalidade" + microcopy de regime econômico. **v0.20.1 fix caixa cartão** — compras de cartão de faturas pagas aparecem detalhadas no Rel 2 (antes só agregado da saída técnica); cache de pagamentos evita O(n²); helper `_rel2ContaSaida(g)` resolve conta via `card.contaPagamentoId`. **v0.20.2 invest na família** — `totaisCategoriaMes` estendida com `invest` separado; módulos Família/Tiago/Config atualizados, eyebrow "consumo" → "gastos". **v0.21.0 compromissos + invest por tipo** — nova seção "compromissos do mês" (coluna stacked fixos + parcelas com legenda lateral) + invest hierárquico (`_relInvestChaveTipo`: idf > grupo > subcat); tag visual indica origem do agrupamento. **v0.21.1 cores compromissos** — fixos terracota + parcelas mostarda `#8A5A1F`. Adicionados 5 itens técnicos: 48 (toggle reformula não esconde), 49 (estruturas paralelas = bug latente), 50 (regimes contábeis precisam microcopy constante), 51 (hierarquia de fallback escala melhor), 52 (refactor sistêmico requer enumeração de callers). Adicionados 3 bullets em "Diferenciação real": regimes contábeis explícitos, compromissos do mês como visão estratégica, invest hierárquico com fallback. 14.784 linhas no `index.html` (+1.111 desde v0.18.1). Próximo: **Fatia 5.4 (Completo, possivelmente trivial agora que 5.1+5.2+5.3 cobrem o universo) ou direto pra 5.5 (Exportação PDF via jsPDF + html2canvas)**.
- **18/maio/2026** — Sessão curta: auditoria de Firestore Security Rules (endurecidas + versionadas no repo como `firestore.rules`; delete do doc raiz bloqueado, subcoleções bloqueadas explicitamente, catch-all defense-in-depth, bateria de 4 testes via Laboratório de Regras passou); patch v0.21.2 com 2 mudanças cromáticas (subcats top 10 do Rel 1 ganham cor da categoria predominante; compromissos passam de terracota+mostarda escura para azul navy `#3A6991` + âmbar `#C28846`); decisão de produto de **pular Fatia 5.4** (Relatório Completo dispensado por design — 5.1+5.2+5.3 já cobrem o universo de perguntas; redundância dilui atenção). Adicionado item técnico 53 (cor como informação, não decoração — princípio de design de dashboards). Adicionado 1 bullet em "Diferenciação real" sobre cor codificando dimensão real dos dados. Validação: `node --check` OK em ambos os scripts inline, 0 NBSPs, 0 triple-backticks, 14.812 linhas. Próximo: **Fatia 5.5 (Exportação PDF via jsPDF + html2canvas)**.
- **18/maio/2026 (noite)** — Sessão de fechamento da Etapa 5: Fatia 5.5 (Exportação PDF) entregue em 3 versões (v0.22.0 → v0.22.2). Estratégia técnica revisada: `window.print()` + CSS `@media print` (PDF vetorial nativo) em vez de `jsPDF + html2canvas` do plano original — texto selecionável, fontes corretas, peso 50-200KB, zero libs externas. Bug visual pré-existente (checkbox como barra branca em todo o app) corrigido preventivamente com override `input[type="checkbox"]` restaurando aparência nativa. Auditoria financeira de divergência Rel 1 vs Família/Compromissos: gap de R$ 2.585,35 era invest fixo HANSARD que o Rel 1 excluía; fix removeu a exclusão pra unificar definição de "compromisso". Princípio violado e restaurado: "uma pergunta financeira = uma resposta consistente, independente de onde for feita". Adicionados itens técnicos 54, 55, 56, 57: uma pergunta = uma resposta canônica; CSS @media print exige seletores reais; appearance:none global quebra checkboxes; window.print() > jsPDF pra PDFs profissionais. Adicionados 2 bullets em "Diferenciação real do produto": auditoria financeira consistente + PDF profissional com texto selecionável. **Etapa 5 COMPLETA** (Fatias 5.1, 5.2, 5.3, 5.5 entregues; 5.4 dispensada). 15.078 linhas (+294 desde v0.21.2). 57 itens em NOTAS-COMERCIAIS. Próximo: Etapa 5b (extrato bancário como gap-filler) ou nova etapa a definir.
- **19–20/maio/2026** — **Etapa 5b (importação de extrato Itaú) + Fatia 5c (Relatório Detalhado) entregues em 4 versões (v0.23.0 → v0.24.0)**. Sessão longa e iterativa atravessando 2 etapas. **v0.23.0** — Fatias 5b.1 + 5b.2 bundled: pipeline completo de importação de extrato bancário com parser Itaú dedicado, arquitetura paralela à de fatura (reusa PDF.js, sheet, parseValor, aliases). 5 tipos automáticos (PAGAMENTO_FATURA com match contra `isPagamentoFatura` existente, ENTRADA_RENDA, ENTRADA_RENDIMENTO, SAIDA_CONSUMO, SAIDA_TARIFA, IGNORAR). Botão "Importar extrato" na tela de detalhe da conta. Parser validado 11/11 contra extrato real do Tiago. Single-source-of-truth da versão via `#appVersionTag`. **v0.23.1** — falha exposta em uso real: Tiago clicou em "aplicar" sem revisar, todos os ~50 gastos foram como "Tiago > a classificar"; sem mecanismo de undo. Fix: schema novo `state.lotesImportacao[]` com FK reversa (gasto.loteImportacaoId); migração retroativa idempotente agrupa órfãos `origem:extrato` num lote de "🛟 Recuperação"; `desfazerLoteImportacao(loteId)` remove records + lote; UI no Config "📥 Histórico de importações"; confirmação modal antes de aplicar; pré-classificação por `buscarAliasParaDescricao` no pipeline de extrato (reuso do sistema de aliases da fatura). **v0.23.2** — segunda falha exposta: Eleva e Wise apareceram como duplicatas não detectadas (gastos manuais sem contaId ou em conta diferente); usuário também não conseguia conferir gastos antigos no app porque a sheet bloqueia; faltava classificação inline. Fix: dedup substituído por detecção em 3 níveis (alta=mesma conta±0,01±2d; média=qualquer conta±0,01±7d; baixa=±1%±15d+palavra-chave); banner inline em vez de exclusão silenciosa; modo rascunho persistido em localStorage (chave `financa.revisaoExtratoDraft`); novo botão "salvar e voltar depois"; picker de classificação inline por linha (modal sheet com chips de categoria + select de subcategoria + select de grupo); aliases aprendidos retroativamente ao aplicar. **v0.24.0 (Fatia 5c)** — decisão estratégica de pausar 5b (Sicoob e BTG ficam pra depois) e entregar nova vista pedida pelo usuário: 4ª aba "Detalhe" em REL_TABS; chips de escopo (Família/Casa/Marina/Heitor/Juliana/Tiago); cartão de resumo (total grande + count + top 5 subcats com %); lista cronológica decrescente agrupada por dia, cada linha com dia/nome/categoria›subcategoria›grupo/método/badges (🔁fixo·💎invest·N/Mparcela·📥extrato·📄fatura·❓aclassificar)/valor; toque abre `openGastoForm`; exclui isAjuste+isPagamentoFatura; PDF com header "Relatório Detalhado · [Escopo]". Adicionados 6 itens técnicos: 58 (lote como FK reversa habilita undo), 59 (dedup probabilístico em 3 níveis), 60 (rascunho persistido pra modal não virar prisão), 61 (classificação inline > apply-then-edit), 62 (aliases aprendidos retroativamente fecham loop), 63 (lista cronológica como complemento a agregados — fato vs métrica). Adicionados 6 bullets em "Diferenciação real": undo em lote, dedup em 3 níveis, modo rascunho, classificação inline + alias retroativo, Relatório Detalhado, livro contábil para auditoria. 16.972 linhas no `index.html` (+1.894 desde v0.22.2). 63 itens em NOTAS-COMERCIAIS. **Próximo: Fatia 5c.1 (filtros + append em Rel 1/2/3) OU Etapa 5b.3 (parser Sicoob) OU Etapa 6 (BLUEPRINT-FINANCA / generalização comercial).**
- **20/maio/2026** — **Fatia 5c.1 entregue (v0.25.0): filtros no Relatório Detalhado + lista detalhada anexável aos PDFs.** Continuação natural da Fatia 5c, escolhida sobre 5b.3 (Sicoob, sem extrato de conta em mãos) e Etapa 6 (generalização prematura). Duas sub-fatias bundled. **(5c.1.a) Filtros no Rel 4** — `rel4State` ganha `filtroSubcats[]` e `filtroGrupos[]`; novos helpers `_rel4AplicarFiltros` (interseção subcat ∩ grupo) e `_rel4Catalogo` (computa as opções de chip a partir da BASE não-filtrada). Header ganha seção "filtrar" com chips multi-seleção (só aparece com ≥2 subcategorias; grupos só se houver). Cartão de resumo recalcula sobre o filtrado. Botão "limpar filtros". Filtros resetam ao trocar escopo, persistem ao navegar meses. Recorte ativo entra no título do PDF ("Heitor · Escola, Esporte"). **(5c.1.b) Lista detalhada nos PDFs do Rel 1 e Rel 3** — toggle "📎 incluir lista detalhada no PDF" (preferência em `localStorage` chave `financa.relPdfApendice`); render cronológico extraído em `_relListaCronologicaHtml(gastos)` reusado por tela (Rel 4) e PDF; `_relApendiceGastos(viewId)` coleta os gastos com o MESMO conjunto+filtro do hero (Rel 1: `relCategoriasIncluidas()`; Rel 3: `['tiago']`; ambos `_relGastoSaida`), respeitando o período selecionado; `_montarApendicePDF`/`_limparApendicePDF` injetam/removem o bloco no `#relApendice` (invisível em tela via CSS, revelado em `@media print` com `page-break-before:always`), limpeza no `afterprint`. **Decisão de escopo:** o append do **Rel 2** ficou de fora — é regime de caixa, inclui entradas (sem categoria/membro) e usa cache de faturas pagas; estrutura diferente que pede renderer próprio. Isolado como **Fatia 5c.2** pra não misturar risco no relatório mais delicado. **Bônus:** ativada a variável CSS `--chip-color` (dead code desde v0.24.0) — chip de escopo ativo agora ganha destaque de cor. Validação: 4 checks (sintaxe/NBSP/backtick/versão) + simulação Node 21/21 (filtros, interseção, técnicos excluídos, invariante apêndice==hero com Tiago OFF/ON e multi-mês). Adicionados 6 itens técnicos: 64 (render parametrizado como fonte única tela/PDF), 65 (reconciliação por construção: detalhe usa mesmo conjunto+filtro do agregado), 66 (apêndice respeita escopo temporal do relatório, não o hero), 67 (filtro multidimensional como interseção + base alimenta catálogo), 68 (token CSS órfão — auditar variáveis sem consumidor), 69 (fatia fina protege o componente mais delicado). Adicionados 2 bullets em "Diferenciação real": filtros finos no Detalhe + agregado-e-detalhe reconciliados no mesmo PDF. 17.169 linhas no `index.html` antes da fatia → ~17.300+ depois. 69 itens em NOTAS-COMERCIAIS. **Próximo: Fatia 5c.2 (append de fluxo no Rel 2) OU Etapa 5b.3 (parser Sicoob, requer extrato de conta) OU Etapa 6 (BLUEPRINT-FINANCA).**
- **20/maio/2026 (noite)** — **Fatias 5d e 5e entregues (v0.26.0 → v0.27.0): relatório elegante compartilhável + extrato cronológico de conferência + classificação com nome/subcategoria.** Duas entregas no mesmo tema-raiz (conferência e compartilhamento). **(5d) v0.26.0** — namespace `window.relatorioElegante`: rota de exportação SEPARADA do "PDF rápido" da Fatia 5.5; gera documento HTML autônomo (Montserrat via @import, capa navy, gráficos SVG, layout A4) em nova aba com barra imprimir/baixar + fallback de download pra popup bloqueado no iOS; abrange Rel 1, Rel 3 (motor do Rel 1 com `cats=['tiago']`) e Rel 2; Rel 4 fica de fora; botão "✦ Relatório" só em rel1/rel2/rel3. Reconcilia por construção (mesmos motores de cálculo das telas). Anti-padrão: sem poupança/runway/%comprometido (reference da skill inclui, SKILL.md proíbe — SKILL.md prevalece). 33/33 testes de reconciliação. **(5e) v0.27.0** — (a) extrato cronológico na tela: botão "🧾 Ver extrato do mês" no fim de Família e Tiago abre sheet com month-nav + total + busca livre (nome/subcat/membro/valor) + lista via `_relListaCronologicaHtml` reusado; Família achata os 4 membros; toque abre `openGastoForm`. (b) picker de classificação do extrato ganha campo "nome/identificação" (vira o `nome` do gasto) + botão "+ nova subcategoria" inline (modo `criandoSub`, evita modal empilhado — picker é z-index 250, askText 200); `aplicarLancamentosExtratoImportados` usa o nome amigável e o ensina ao alias via `identificacao` (campo que existia mas nunca era alimentado); auto-classificação por alias passa a aplicar `alias.identificacao` como nome. Busca 9/9. Adicionados 5 itens técnicos: 70 (mapear template de skill ao schema real antes de usar), 71 (SKILL.md prevalece sobre reference file em conflito), 72 (componente de render pertence ao app, não ao relatório — reuso em superfície diária), 73 (criar entidade inline > empilhar 2º modal — z-index frágil), 74 (capacidade latente no schema — campo cabeado pela metade). Adicionados 3 bullets em "Diferenciação real": relatório elegante compartilhável, extrato de conferência com busca, classificação que aprende o apelido. Validação nas duas: 4 checks da skill de entrega (sintaxe/NBSP/backtick/versão). `index.html` ~17.300 → ~18.450 linhas. 74 itens em NOTAS-COMERCIAIS. Decisões de escopo abertas: subcategoria nova nasce exclusiva do membro (não universal), extrato lista só gastos (não entradas). **Próximo: Fatia 5c.2 (append de fluxo no Rel 2) OU Etapa 5b.3 (parser Sicoob, requer extrato de conta) OU Etapa 6 (BLUEPRINT-FINANCA) OU estender nome+nova-subcat ao picker de FATURA (`renderRevisaoItem`).**
- **21/maio/2026** — **Sessão de unificação de relatórios + parser de extrato Sicoob (5 fatias entregues, v0.29.0 → v0.33.0; Etapa 5 FECHADA).** **(5b.3/v0.29.0)** Parser de extrato de CONTA Sicoob — segundo banco no framework de extrato (depois do Itaú), distinto dos parsers de fatura. Layout próprio (data DD/MM sem ano, valor com sufixo D/C/* colado, descrição multi-linha, corte da seção RESUMO+LANÇAMENTOS FUTUROS). Classificação estendida por bloco guardado em `if (l.historico)` (campo exclusivo Sicoob — Itaú intacto); pagamento de fatura Mastercard detectado pra não duplicar. **Conciliação de saldo fechou em R$ 0,00** (32 lançamentos de maio). Validado por sandbox isolado em Node contra o texto real do PDF, depois transplante verificado. **(5c.2/v0.30.0)** Fluxo de caixa detalhado no Rel 2 elegante (entradas/saídas/invest cronológicos, reconcilia com a capa) — redirecionada da ideia original (PDF rápido) pro elegante por decisão de unificação. **(5c.3/v0.31.0)** Gastos detalhados por categoria no Rel 1 elegante (membro com total → subcategoria com subtotal → itens). **(5c.4/v0.32.0)** Caixa única com 3 toggles empilhados + pref `relPdfDetalheCategoria` (default ON, desliga em períodos longos). **(5c.5/v0.33.0)** Lista cronológica no Rel 1/3 elegante, controlada pelo toggle "lista detalhada" — coerência "o que liga é o que sai". Etapa 5 declarada FECHADA; backlog: Rel 4 elegante, aposentar PDF rápido, caixa de toggles no Rel 3. Adicionados 7 itens técnicos: 75 (conciliação de saldo como audit de ouro do extrato), 76 (classificação por campo exclusivo do banco — extensibilidade sem regressão), 77 (validar parser isolado em sandbox antes de transplantar), 78 (**dashboard vivo × relatório estático** — distinção de produto chave pra versão de empresa), 79 (unificação progressiva — crescer o bom até aposentar o ruim), 80 (controle agrupado que rege tela e saída juntos), 81 (paridade entre superfícies que fazem a mesma tarefa — inclui a Fatia 5e.1/v0.28.0, que não tinha entrada própria, sobre paridade do picker extrato↔fatura). Adicionados 2 bullets em "Diferenciação real": importação de extrato com conta que fecha (R$ 0,00) + relatório elegante com profundidade sob demanda. 81 itens em NOTAS-COMERCIAIS. Prompt Mestre consolidado em V21. **Próximo: backlog da unificação (Rel 4 elegante → aposentar PDF rápido) OU Etapa 5b.4 (extrato BTG, requer extrato de conta real) OU Etapa 6 (BLUEPRINT-FINANCA / versão de empresa).**
